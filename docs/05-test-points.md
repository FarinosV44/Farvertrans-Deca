# Test Points — Farvertrans DeCA

| Date | Slice / issue | Criterion | Coverage | Red first | Command | Result | Evidence (commit) |
|---|---|---|---|---|---|---|---|
| 2026-09-03 | BUILD 05 scaffold | app boots, `/` renders, one h1, CTA→/crear, no console errors, no 5xx | driven (Playwright smoke, 2/2 pass) | n/a (scaffold) | `npm run test:e2e` | PASS (2 passed, 39.8s) | 7719c- (next commit) |
| 2026-09-03 | BUILD 05 scaffold | env parsing rules (bad URL, short secret, FVD_DEBUG coercion) | driven (Vitest, 3/3 pass) | n/a (mirrors lib/env) | `npm run test:unit` | PASS (3 passed) | 7719c- |
| 2026-09-03 | BUILD 05 scaffold | Prisma schema validates + migrates + seeds | driven | n/a | `npx prisma validate` / `migrate dev` / `npm run seed` | PASS (migration 20260903114315_init applied; 4 operators seeded) | 7719c- |
| 2026-09-03 | BUILD 05 scaffold | lint + typecheck + build + keel-verify pass | driven | n/a | `npm run lint` / `npm run typecheck` / `npm run build` / `node scripts/keel-verify.mjs` | PASS (all clean) | 7719c- |
| 2026-09-03 | BUILD 05 scaffold | health endpoint | driven (Playwright) | n/a | `GET /health` | PASS (200, version + db reported) | 7719c- |
| 2026-09-03 | BUILD 06 landing | AC-25 SSR, one h1 "DeCA GRATIS", CTA→/crear (not contact) | driven (Playwright) | n/a | `npm run test:e2e` | PASS | next commit |
| 2026-09-03 | BUILD 06 landing | AC-26 no pricing/contact/demo/sales gating; 0 `<form>` | driven | n/a | e2e landing.spec | PASS | next commit |
| 2026-09-03 | BUILD 06 landing | AC-27 content + primary CTA work with JS disabled | driven (jsEnabled:false) | n/a | e2e landing.spec | PASS | next commit |
| 2026-09-03 | BUILD 06 landing | AC-32 title/meta/canonical/OG present; robots disallows /api /d/; sitemap public-only | driven | n/a | e2e landing.spec | PASS | next commit |
| 2026-09-03 | BUILD 06 landing | AC-33 JSON-LD SoftwareApplication + FAQPage | driven | n/a | e2e landing.spec | PASS | next commit |
| 2026-09-03 | BUILD 06 landing | AC-31 landing_view event emitted + accepted (204) | driven | n/a | e2e landing.spec | PASS | next commit |
| 2026-09-03 | BUILD 06 landing | AC-29 no serious/critical axe violations on / and /crear; CTA keyboard-reachable | driven (@axe-core/playwright) | n/a | e2e a11y.spec | PASS | next commit |
| 2026-09-03 | BUILD 06 landing | responsive: no horizontal overflow at 360/768/1280; mobile CTA shows <768 only | driven | n/a | e2e landing.spec | PASS | next commit |
| 2026-09-03 | BUILD 06 landing | pickRefSnapshot / eventInputSchema / landing JSON-LD builder | driven (Vitest 7 tests) | pure logic — analytics + content builders | `npm run test:unit` | PASS (10 unit total) | 7262ca1 |
| 2026-09-03 | BUILD 07 creator | validateDeca R-2/AC-09 (missing-field rejection, foreign-NIF warn-not-block, date format, path-keyed errors); normalizePlate; checkNif | driven (Vitest, 10 tests) | logic written from R-2/AC-09, not the impl; tests pass on first run (velocity note — no observed red) | `npm run test:unit` | PASS (20 unit total) | next commit |
| 2026-09-03 | BUILD 07 creator | AC-01 anonymous completes 3 steps → /crear/[id] "DeCA generado" | driven (Playwright) | n/a | e2e crear.spec | PASS | next commit |
| 2026-09-03 | BUILD 07 creator | AC-02 mandatory omission blocks advance, Spanish message, accessible error summary, stays on step | driven | n/a | e2e crear.spec | PASS | next commit |
| 2026-09-03 | BUILD 07 creator | back/forward preserves entered data (sessionStorage draft) | driven | n/a | e2e crear.spec | PASS | next commit |
| 2026-09-03 | BUILD 07 creator | AC-09 POST /api/deca 422 + field errors on missing mandatory field | driven | n/a | e2e crear.spec | PASS | next commit |
| 2026-09-03 | BUILD 07 creator | AC-01 POST /api/deca 201 + ≥256-bit token + claim token for anonymous | driven | n/a | e2e crear.spec | PASS | next commit |
| 2026-09-03 | BUILD 07 creator | AC-04b same idempotency-key never creates a 2nd DeCA | driven | n/a | e2e crear.spec | PASS | next commit |
| 2026-09-03 | BUILD 07 creator | AC-04 wizard axe-clean; progressbar named; error summary role=alert focus | driven (@axe-core) | n/a | e2e a11y.spec + crear.spec | PASS | 7641709 |
| 2026-09-03 | BUILD 08 PDF/QR/URL | R-5/AC-07 QR encodes URL X → jsQR decodes back to exactly X | driven (Vitest, pngjs+jsqr) | pure logic — QR round-trip | `npm run test:unit` | PASS | next commit |
| 2026-09-03 | BUILD 08 PDF/QR/URL | R-8 public token: ≥40 url-safe chars, unique per call | driven (Vitest) | pure logic | `npm run test:unit` | PASS | next commit |
| 2026-09-03 | BUILD 08 PDF/QR/URL | R-9 availability window: available until 7 natural days after service end, then not | driven (Vitest) | pure logic — written from R-9 | `npm run test:unit` | PASS | next commit |
| 2026-09-03 | BUILD 08 PDF/QR/URL | R-3/R-4 PDF is native text (all R-2 values extractable via pdfjs), starts %PDF-, < 5 MB (~20 KB) | driven (Playwright + pdfjs) `test:compliance` | n/a | `npm run test:compliance` | PASS | next commit |
| 2026-09-03 | BUILD 08 PDF/QR/URL | R-5/R-6 PDF carries the exact /d/[token] URL as text | driven | n/a | test:compliance | PASS | next commit |
| 2026-09-03 | BUILD 08 PDF/QR/URL | R-7/R-8 /d/[token] streams the PDF directly: application/pdf, inline, noindex, no set-cookie, not HTML | driven | n/a | test:compliance | PASS | next commit |
| 2026-09-03 | BUILD 08 PDF/QR/URL | R-8 unknown token → generic 404 text/plain | driven | n/a | test:compliance | PASS | next commit |
| 2026-09-03 | BUILD 08 PDF/QR/URL | R-11 PDF metadata records CreationDate + Creator="Farvertrans DeCA v..." | driven (pdfjs getMetadata) | n/a | test:compliance | PASS | next commit |
| 2026-09-03 | BUILD 08 PDF/QR/URL | R-13 fails closed — invalid payload → 422, no document | driven | n/a | test:compliance | PASS | next commit |
| 2026-09-03 | BUILD 08 PDF/QR/URL | standalone build traces lib/pdf/fonts into .next/standalone (Hostinger Docker) | driven | n/a | `NEXT_STANDALONE=1 npm run build` + ls | PASS | 206d734 |
| 2026-09-03 | BUILD 09 signup+claim | password scrypt hash/verify (correct/wrong/malformed); min length | driven (Vitest) | pure logic | `npm run test:unit` | PASS | next commit |
| 2026-09-03 | BUILD 09 signup+claim | session token HMAC sign/verify; tamper + junk rejected | driven (Vitest) | pure logic | `npm run test:unit` | PASS (31 unit total) | next commit |
| 2026-09-03 | BUILD 09 signup+claim | AC-17 generate first → register second → DeCA claimed, appears in /app | driven (Playwright) | n/a | e2e registro.spec | PASS | next commit |
| 2026-09-03 | BUILD 09 signup+claim | auth failure (422) never orphans/deletes the DeCA; claim token still usable after | driven | n/a | e2e registro.spec | PASS | next commit |
| 2026-09-03 | BUILD 09 signup+claim | a claim token cannot be reused by a second account (reports already-used) | driven | n/a | e2e registro.spec | PASS | next commit |
| 2026-09-03 | BUILD 09 signup+claim | signup form carries no lead-qual fields (flota/facturación/empleados/teléfono/demo/cargo) | driven | n/a | e2e registro.spec | PASS | next commit |
| 2026-09-03 | BUILD 09 signup+claim | /registro axe-clean (no serious/critical) | driven (@axe-core) | n/a | e2e a11y.spec | PASS | next commit |
| 2026-09-03 | BUILD 09 signup+claim | /registro axe-clean (no serious/critical) | driven (@axe-core) | n/a | e2e a11y.spec | PASS | fc5609a |
| 2026-09-03 | BUILD 10 workspace | history filter predicate (free text across ref/place/carrier/plate/shipper; date range) | driven (Vitest) | pure logic | `npm run test:unit` | PASS (36 unit) | next commit |
| 2026-09-03 | BUILD 10 workspace | saved-entity schemas (plate normalisation, NIF required) | driven (Vitest) | pure logic | `npm run test:unit` | PASS | next commit |
| 2026-09-03 | BUILD 10 workspace | authed DeCA is owned by the company + appears in /app + /app/historico | driven (Playwright) | n/a | e2e workspace.spec | PASS | next commit |
| 2026-09-03 | BUILD 10 workspace | history search filters (q match / no-match, date range) | driven | n/a | e2e workspace.spec | PASS | next commit |
| 2026-09-03 | BUILD 10 workspace | "Repetir último DeCA" / duplicate prefills, date reset, NEW id/token on generate | driven | n/a | e2e workspace.spec | PASS | next commit |
| 2026-09-03 | BUILD 10 workspace | saved data add → autofill in wizard (company + vehicle) → delete; a generated DeCA is untouched | driven | n/a | e2e workspace.spec | PASS | next commit |
| 2026-09-03 | BUILD 10 workspace | /app, /app/historico, /app/datos axe-clean | driven (@axe-core) | n/a | e2e workspace.spec | PASS | next commit |
| 2026-09-03 | BUILD 11 attribution | parseTouch: referral/campaign/organic/direct classification + all 5 UTMs captured | driven (Vitest) | pure logic — written from F12/EPIC 02 | `npm run test:unit` | PASS (43 unit) | next commit |
| 2026-09-03 | BUILD 11 attribution | merge rules: first-touch captured once + NEVER overwritten after lock; last-touch updates on qualifying touch only; no-op once locked; acquisition-row flatten | driven (Vitest, 5 tests) | pure logic | `npm run test:unit` | PASS | next commit |
| 2026-09-03 | BUILD 11 attribution | AC-18/19 ?ref=adrian → browse → anonymous flow → signup attributed to adrian; user never sees operator name | driven (Playwright) | n/a | e2e attribution.spec | PASS | next commit |
| 2026-09-03 | BUILD 11 attribution | AC-20 return via ?ref=maria before signup → last-touch maria, first stays adrian | driven | n/a | e2e attribution.spec | PASS | next commit |
| 2026-09-03 | BUILD 11 attribution | AC-21/22 no ref + no UTM → organic/direct, still recorded | driven | n/a | e2e attribution.spec | PASS | next commit |
| 2026-09-03 | BUILD 11 attribution | AC-23 per-operator companies/first-DeCA/total-DeCA queryable via /api/operadores/stats (internal only) | driven | n/a | e2e attribution.spec | PASS | next commit |
| 2026-09-03 | BUILD 11 attribution | first_deca_at set when an attributed company generates its first DeCA (authed create + claim) | driven | n/a | e2e attribution.spec | PASS | next commit |
| 2026-09-03 | BUILD 12 dashboard | non-internal user cannot discover /operadores (404) nor /api/operadores/stats (404) — anon + regular user | driven (Playwright) | n/a | e2e operadores.spec | PASS | next commit |
| 2026-09-03 | BUILD 12 dashboard | internal user sees the operator table; per-operator companies/first-DeCA/total-DeCA reconcile with the API | driven | n/a | e2e operadores.spec | PASS | next commit |
| 2026-09-03 | BUILD 13 abuse | decide(): first-time user allowed (no challenge); soft threshold → challenge; hard → block+retry-after | driven (Vitest) | pure logic — F16 | `npm run test:unit` | PASS (47 unit) | next commit |
| 2026-09-03 | BUILD 13 abuse | PoW challenge: valid nonce (plain SHA-256) verified; wrong/forged-prefix/wrong-scope/oversize rejected | driven (Vitest) | pure logic | `npm run test:unit` | PASS | next commit |
| 2026-09-03 | BUILD 13 abuse | AC-34/35 anonymous: first 3 creates never challenged; 4th → 429 challenge; solved PoW retry → 201 | driven (Playwright API) | n/a | e2e build13.spec | PASS | next commit |
| 2026-09-03 | BUILD 13 abuse | AC-36 `GET /d/[token]` never returns 429 (15 rapid fetches all 200) | driven | n/a | e2e build13.spec | PASS | next commit |
| 2026-09-03 | BUILD 13 versioning | AC-14/15 correction → new `deca_version` (new token/QR/PDF), reason + "Versión actual: 2"; v1 PDF still retrievable with original destination; distinct tokens per version | driven (Playwright + pdfjs) | n/a | e2e build13.spec | PASS | next commit |
| 2026-09-03 | BUILD 13 versioning | AC-16 non-owner cannot correct (401 from an unauthenticated context) | driven | n/a | e2e build13.spec | PASS | next commit |
| 2026-09-03 | BUILD 13 sharing | AC-24 share panel: WhatsApp deep link contains the `/d/` URL; email via `POST /api/share` (rate-limited, templated, mailto fallback when unconfigured); `deca_shared` event | driven | n/a | e2e build13.spec | PASS | next commit |
| 2026-09-03 | BUILD 14 SEO | 10 core pages: SSR (JS disabled), exactly one h1, unique <title>, canonical, meta description, FAQPage JSON-LD, CTA→/crear, internal cluster links, last-reviewed date | driven (Playwright, jsEnabled:false) | n/a | e2e seo.spec | PASS | next commit |
| 2026-09-03 | BUILD 14 SEO | an unknown slug 404s (dynamicParams=false) | driven | n/a | e2e seo.spec | PASS | next commit |
| 2026-09-03 | BUILD 14 SEO | sitemap lists all 10 + /soy-obligado, no /app /api /d/; robots disallows private areas | driven | n/a | e2e seo.spec | PASS | next commit |
| 2026-09-03 | BUILD 14 SEO | "¿Estoy obligado?" returns a conclusion with JavaScript disabled (SSR query-param form) | driven | n/a | e2e seo.spec | PASS | next commit |
| 2026-09-03 | BUILD 14 SEO | representative SEO page axe-clean (no serious/critical) | driven (@axe-core) | n/a | e2e seo.spec | PASS | next commit |
| 2026-09-03 | BUILD 15 gate | no cross-tenant access: company B → 404 on A's /app/deca/[id], /corregir, /api/deca/[id]/version; B's history has 0 of A's docs | driven (Playwright, 2 contexts) | n/a | e2e launch-gate.spec | PASS | next commit |
| 2026-09-03 | BUILD 15 gate | security headers on HTML: CSP (default-src 'self', frame-ancestors 'none', no unsafe-eval in prod), nosniff, X-Frame-Options DENY, Referrer-Policy, Permissions-Policy | driven | n/a | e2e launch-gate.spec | PASS | next commit |
| 2026-09-03 | BUILD 15 gate | public tokens ≥240-bit base64url, unique, adjacent guess → 404 | driven | n/a | e2e launch-gate.spec | PASS | next commit |
| 2026-09-03 | BUILD 15 gate | failure paths: malformed create → 422, bad beacon → 204 (never 5xx), unknown token → 404, bad share → 422 | driven | n/a | e2e launch-gate.spec | PASS | next commit |
| 2026-09-03 | BUILD 15 gate | full compliance suite re-run on the build | driven | n/a | `npm run test:compliance` | PASS (6/6 R-1…R-13) | next commit |
| 2026-09-03 | BUILD 15 gate | Docker standalone production build + keel-verify + format:check | driven | n/a | `NEXT_STANDALONE=1 npm run build` / `npm run format:check` | PASS | next commit |
| 2026-09-08 | #85 pt1 WhatsApp channel | commercial channel shows "WhatsApp" not "Teléfono"; `commercialChannelLabelEs()` maps phone→WhatsApp, both→"Correo y WhatsApp"; admin uses it | driven (Vitest) | pure logic — helper | `npm run test:unit tests/unit/commercial-availability.test.ts` | PASS (11) | this commit |
| 2026-09-08 | #85 pt1 WhatsApp channel | /panel/privacidad channel select renders "WhatsApp", never "Teléfono" | driven (Playwright) | n/a | `npx playwright test tests/e2e/commercial-consent.spec.ts` | see run below | this commit |
| 2026-09-08 | #85 pt2 saved-company address | `savedCompanySchema` keeps optional postalCode/city; empty when omitted; rejects >12-char CP | driven (Vitest) | observed — new pure-logic assertions written against the field addition | `npm run test:unit tests/unit/saved-schema.test.ts` | PASS (3) | this commit |
| 2026-09-08 | #85 pt2 saved-company address | picking a saved company in the wizard fills shipper/carrier CP + población into the DeCA party | driven (Playwright) | n/a | `npx playwright test tests/e2e/master-data.spec.ts` | see run below | this commit |
| 2026-09-08 | #85 pt3 launch pricing | landing renders the "Gratis durante 2026" badge; no "gratis para siempre" phrasing; FAQ names the 2027 subscription | driven (Playwright) | n/a | `npx playwright test tests/e2e/landing.spec.ts` | see run below | this commit |
| 2026-09-08 | #85 full slice | typecheck + lint + prettier + full unit suite + keel:verify | driven | n/a | `npx tsc --noEmit` / `npm run lint` / `npx vitest run` / `node scripts/keel-verify.mjs` | PASS (205 unit, lint pre-existing warnings only) | this commit |
| 2026-09-08 | #87 corridors/activity | zoneOf (ES provinces → regional zones, foreign countries, city hints), matchCorridors (Valencia→Lyon = ES↔FR both directions), summariseActivity (7/30/60/90d windows, trend vs prior 30d, weekday histogram) | driven (Vitest) | pure logic — test-first (D-014) | `npm run test:unit tests/unit/commercial-corridors.test.ts tests/unit/commercial-activity.test.ts` | PASS (23) | this commit |
| 2026-09-08 | #87 opportunity model | applyOpportunityFilter (q/state/operator/corridor/destination-across-routes/activity), sortOpportunities (recent/recurrence/volume), deriveOpportunity (movements, nextUnloadDate, contactable) | driven (Vitest) | pure logic | `npm run test:unit tests/unit/commercial-opportunities.test.ts` | PASS (16) | this commit |
| 2026-09-08 | #88 affinity/profile | affinityScore (clamped 0-100, band thresholds, every point a labelled rule, monotonic on adding a contact), autoTags (objective), summariseRoutes (grouping, tallies, 2+ plate, corridor ids) | driven (Vitest) | pure logic — test-first | `npm run test:unit tests/unit/commercial-affinity.test.ts tests/unit/commercial-carrier-profile.test.ts` | PASS (12) | this commit |
| 2026-09-08 | #87/#88 radar + profile e2e | a consented carrier appears on `/admin/oportunidades` with its route activity + WhatsApp action; filter by destination; manual state persists (DB check); `/admin/empresas/[id]` shows the affinity score + breakdown; a NON-consented carrier never appears and has no derived profile; API 404 non-internal, 409 non-eligible | driven (Playwright + Prisma) | n/a | `npx playwright test tests/e2e/commercial-intelligence.spec.ts` | PASS (2) | this commit |
| 2026-09-08 | admin content editor fix (D-153) | create a BLOG draft in `/admin/contenido/nuevo` → lands on the editable detail page with the "Publicar" button → publish → "published" badge + public `/blog/[slug]` 200 | driven (Playwright) | reproduced the user report (soft-nav cache) then fixed with hard navigation | `npx playwright test tests/e2e/content-cms.spec.ts` | PASS (6/6, was a documented flake) | this commit |
| 2026-09-08 | #87+#88 full slice | typecheck + lint + prettier + full unit + production build + full e2e | driven | n/a | `npx tsc --noEmit` / `npm run lint` / `npx vitest run` / `npm run build` / `npx playwright test` | PASS (273 unit; e2e 232 passed / 2 documented flakes green at --workers=1) | this commit |
| 2026-09-08 | #89 KPIs | rollupFunnel (per-state counts, always every state in funnel order), maxProgress (furthest funnel point ever reached, not just current state; terminal-only = -1) | driven (Vitest) | pure logic | `npm run test:unit tests/unit/commercial-kpis.test.ts` | PASS (4) | this commit |
| 2026-09-08 | #90 alert rules | evaluateAlerts — each of the 10 rules fires on its documented condition and NOT otherwise (upcoming priority zone, first corridor, recurrence, growing, uncontacted, stale follow-up, reactivation, referral first DeCA, acquired recurring); quiet carrier → no alerts; dedupeKey stable per company+kind(+week) | driven (Vitest) | pure logic — test-first | `npm run test:unit tests/unit/commercial-alert-rules.test.ts` | PASS (12) | this commit |
| 2026-09-08 | #89/#90 e2e | conversion PATCH writes a whatsapp/converted activity-log entry + the manual outcome + convertedByUserId; `/admin/comercial` shows the funnel + attributed revenue + the activity note; the #88 ficha shows the commercial history; `/admin/alertas-comerciales` surfaces rule-based alerts for a consented carrier and dismissed alerts stay dismissed after a recompute | driven (Playwright + Prisma) | n/a | `npx playwright test tests/e2e/commercial-intelligence.spec.ts` | PASS (2, at --workers=1) | this commit |
| 2026-09-08 | #89+#90 full slice | typecheck + lint + prettier + full unit + production build + full e2e | driven | n/a | `npx tsc --noEmit` / `npm run lint` / `npx vitest run` / `npm run build` / `npx playwright test` | PASS (289 unit; e2e 231 passed / 3 internalPage-contention flakes, all green isolated) | this commit |
| 2026-09-09 | #94 P0 — internal pages closed on every transport | anon / normal customer / internal × 3 RSC transports over all 29 `(protected)` pages + 7 detail routes; plain nav still 404 | driven (Playwright, 6/6) | **observed** — pre-fix run failed on the real leak: `/admin/empresas leaked a real company record to an anonymous caller` and `... leaked its own rendered payload ...`; anon RSC returned 200 with 478 KB / 240 company-name hits (`/admin/activacion` 7.74 MB / 6080) | `npx playwright test tests/e2e/admin-rsc-authz.spec.ts` | PASS (6 passed) | bf3ce0f |
| 2026-09-09 | #94 — the class cannot reopen | every `app/admin/(protected)/**/page.tsx` calls a guard of its own | driven (keel-verify) | n/a — out of scope (a lint check, not product logic) | `node scripts/keel-verify.mjs` | PASS (`all 29 internal pages call requireInternal() themselves (#94)`) | bf3ce0f |
| 2026-09-09 | #92 — saved Histórico views (pure logic) | filter set is exactly the page's own; params→filters; canonical query; active-chip match; name validation; summary | driven (Vitest, 26/26) | **observed** — module absent, suite failed to collect before `lib/data/history-views.ts` existed | `npx vitest run tests/unit/history-views.test.ts` | PASS (26 passed) | (this commit) |
| 2026-09-09 | #93 — quick-access catalogue (pure logic) | catalogue has no duplicate destinations; selection normalised (unknown/repeat/4th dropped); defaults for an unconfigured user; retired key repaired | driven (Vitest, 14/14) | **observed** — module absent, suite failed to collect before `lib/panel/quick-actions.ts` existed | `npx vitest run tests/unit/panel-quick-actions.test.ts` | PASS (14 passed) | (this commit) |
| 2026-09-09 | #92 — saved views end to end | save from active filters, apply in one click, rename, delete, persistence across reload, duplicate-name error shown, private to the user, anonymous 401, another user's id 404 (no IDOR) | driven (Playwright, 5 tests) | n/a — policy (`pure-logic`; the pure half is the two rows above) | `npx playwright test tests/e2e/panel-personalisation.spec.ts` | PASS (9 passed incl. #93) | (this commit) |
| 2026-09-09 | #93 — quick accesses end to end | defaults shown, editable, 4th option disabled at the cap, persists across reload, shortcut navigates, restore defaults, per-user isolation, junk normalised server-side, anonymous 401 | driven (Playwright, 4 tests) | n/a — policy (`pure-logic`) | `npx playwright test tests/e2e/panel-personalisation.spec.ts` | PASS (9 passed incl. #92) | (this commit) |
| 2026-09-09 | #92 + #93 + #94 — full regression | the whole suite, nothing regressed; a11y unchanged on `/panel`, `/panel/historico`, `/panel/datos` | driven (Playwright, 249/249) | n/a — policy | `npm run typecheck && npm run lint && npx vitest run && npx playwright test` | PASS (330 unit, 249 e2e, 0 failed) | (this commit) |
| 2026-09-09 | D-159 — #84 registration opt-in restyled (presentation only) | commercial-opt-in checkbox: unchecked by default, all 8 acceptance cases, withdrawal — unchanged behaviour under the new markup | driven (Playwright, 14/14, existing suite unmodified) | n/a — predates (UI copy/style change over an existing, already-covered control; no new logic) | `npx playwright test tests/e2e/commercial-consent.spec.ts` | PASS (14 passed) | (this commit) |
| 2026-09-09 | #102 — pickFallbackMembership (pure logic) | which remaining membership becomes active when the current one is removed | driven (Vitest, 5/5) | **observed** — module absent, suite failed to collect before `lib/team-membership.ts` existed | `npx vitest run tests/unit/membership-fallback.test.ts` | PASS (5 passed) | (this commit) |
| 2026-09-09 | #102 — exact reported bug reproduced then fixed | invite-accept overwriting an existing company; remove-member orphaning the account; idempotent re-accept; no extra company for a new user; account survives removal | driven (Playwright, 5/5) | n/a — policy (integration flow, not pure logic) | `npx playwright test tests/e2e/membership.spec.ts` | PASS (5 passed) | (this commit) |
| 2026-09-09 | #102 — existing team.spec.ts regression check | invite/accept/role-change/remove flows unaffected by the Membership rewrite | driven (Playwright, 7/7, unmodified except the new confirm dialog) | n/a — policy | `npx playwright test tests/e2e/team.spec.ts` | PASS (7 passed) | (this commit) |
| 2026-09-09 | #102 — full regression | nothing else regressed | driven (Playwright, 254 total) | n/a — policy | `npx playwright test --workers=3` | PASS (253/254; 1 = the documented `master-data.spec.ts:38` contention flake, green isolated) | (this commit) |
| 2026-09-09 | #98 — structured data audit + fix | FAQPage removed (violated the issue's own instruction); WebSite added; Organization taxID/logo added; Article image added when present; escaping normalized | driven (Vitest 336/336, Playwright landing.spec.ts 14/14 + seo-regression.spec.ts 23/23) | **observed** — `landing-content.test.ts` and `landing.spec.ts:48` (AC-33) both failed for the right reason against the pre-fix code (asserted the now-removed FAQPage), then rewritten to assert its absence | `npx vitest run && npx playwright test tests/e2e/landing.spec.ts tests/e2e/seo-regression.spec.ts` | PASS | (this commit) |
| 2026-09-09 | D-170 — #103 correction: no irreversible company action in Superadmin | anonymize/delete/hard_delete all rejected (422) for companies; block→reactivate still works; AccountActions hides the anonymize UI for kind=empresas | driven (Playwright, admin-account-lifecycle.spec.ts 7/7) | **observed** — the pre-existing "anonymise a company" test failed for the right reason once the action was removed (its assertions expected 200/success), then rewritten to assert rejection | `npx playwright test tests/e2e/admin-account-lifecycle.spec.ts` | PASS (7/7) | (this commit) |
| 2026-09-09 | D-171 — #97 internal-linking architecture (pure logic) | hub/strategic slugs are real `content/seo/pages.ts` slugs, no duplicates; `pickCornerstones` never self-links, skips already-linked, deterministic; `suggestRelatedByCategory` matches case/whitespace-insensitively, excludes self, caps at limit; the audit script's `STRATEGIC_ROUTES` copy stays in sync with the lib | driven (Vitest, 11/11) | **observed** — module absent, suite failed to collect before `lib/content/internal-linking.ts` existed | `npx vitest run tests/unit/internal-linking.test.ts` | PASS (11 passed) | (this commit) |
| 2026-09-09 | D-171 — #97 related-content picker end to end | same-category suggestion appears and is addable once (disabled once added); manual filterable pick works independently; both selections survive the save round-trip; the published article's public "Sigue leyendo" block links the selected guide | driven (Playwright, content-cms.spec.ts) | **observed** — `data-testid="ce-related-*"` did not exist before the picker UI was added; `relatedSlugs` was previously write-only (saved, never settable from the UI) | `npx playwright test tests/e2e/content-cms.spec.ts --workers=1` | PASS (6/6) | (this commit) |
| 2026-09-09 | D-171 — #97 internal-links audit script, run against a live build | orphan pages, thin strategic-page inbound linking, broken internal links, repeated-anchor smell, link-heavy pages, click depth from home | driven (manual run against `next start`) | n/a — new diagnostic script, not a red/green unit | `npm run seo:links-audit` | 0 broken links, 0 orphans, 4 expected hub-reinforcement anchor repeats (non-blocking), 2 strategic pages flagged thin for editorial follow-up | (this commit) |
| 2026-09-09 | #97 — full regression | nothing else regressed (347 unit; landing/seo-regression/content-cms e2e suites green; one documented `internalPage`-contention flake on `commercial-intelligence.spec.ts`, green isolated) | driven (Vitest 347/347, Playwright 282/283 parallel + 2/2 isolated) | n/a — policy | `npx vitest run && npx playwright test` | PASS | (this commit) |
| 2026-09-09 | D-172 — #96 CWV: `SiteHeader`'s `locale` prop + `LanguageSwitcher`'s client-side text swap (pure/behavioural) | `header-strings.ts` matches the full dictionaries verbatim in all 8 locales (drift-detection); a static page defaults to Spanish and updates with no navigation on switch; a returning visitor's existing non-Spanish cookie is honoured on load; the still-dynamic landing page is unaffected | driven (Vitest 10/10, Playwright 4/4) | **observed** — `tests/unit/header-strings.test.ts` failed to collect before the file existed; `tests/e2e/i18n-header.spec.ts` exercised `data-testid="header-login"` text before `SiteHeader`'s `locale` prop and the swap logic existed, and it showed the old always-cookie-driven text | `npx vitest run tests/unit/header-strings.test.ts && npx playwright test tests/e2e/i18n-header.spec.ts --workers=1` | PASS (10 unit, 4 e2e) | (this commit) |
| 2026-09-09 | D-172 — #96 CWV: root-cause verification against a real build | confirms the actual, remaining cause of site-wide `Cache-Control: no-store` after the `SiteHeader` fix — `curl` against `npm run build && next start`, with and without an existing `fvd_attr`/locale cookie, and with `middleware.ts` temporarily excluding the test route (ruled out) | driven (manual, against a real production build — not a red/green unit) | n/a — root-cause investigation, not a regression test | `npm run build && npm run start` + `curl -sI` | Confirmed: `app/layout.tsx`'s own `getLocale()` call is the remaining, deeper cause — documented in D-172, not fixed in this slice | (this commit) |
| 2026-09-09 | #96 (D-172 slice) — full regression | nothing else regressed | driven (Vitest 357/357, Playwright — landing/seo-regression/content-cms/i18n-header suites green) | n/a — policy | `npx vitest run && npx playwright test tests/e2e/landing.spec.ts tests/e2e/seo-regression.spec.ts tests/e2e/content-cms.spec.ts tests/e2e/i18n-header.spec.ts --workers=1` | PASS | (this commit) |
| 2026-09-09 | D-173 — URGENT: company-less logged-in user's dead-end loop | removed member, still logged in, visits `/panel` → lands on `/registro/completar-empresa` (not the full signup form), completes a company, reaches a working `/panel` | driven (Playwright, membership.spec.ts extended) | **observed** — `git stash` against the pre-fix `app/panel/**` files reproduced the exact reported loop (`/panel` → `/registro`, dead end) before the fix; green after | `npx playwright test tests/e2e/membership.spec.ts --workers=1` | PASS (5/5) | f018160 |
| 2026-09-09 | D-173 — full regression | nothing else regressed; 2 pre-existing `account.spec.ts` assertions correctly updated to the new, equally-correct final destination (`/entrar`) for a fully invalidated session | driven (Vitest 357/357, Playwright 285/286 workers=3 + isolated re-check) | n/a — policy | `npx vitest run && npx playwright test --workers=3` | PASS | f018160 |
| 2026-09-09 | D-174 — #96 CWV: hero-image CLS fix + Inter font removal + performance budgets | `article-layout.tsx`'s `heroImage` reserves its box before load; Inter fully removed with zero other references left; `scripts/perf-budget.mjs` correctly parses a real build's route table and checks against measured budgets | driven (manual run against a real build; no automated red/green — a diagnostic script, same class as `seo-audit.mjs`) | n/a — new script, not a regression fix | `npm run perf:budget` | 8/8 priority routes within budget (`/` 127 kB, `/crear` 210 kB, `/entrar`+`/registro` 201 kB, `/guias` 128 kB, `/blog`+`/soy-obligado`+`/[slug]` 126 kB) | (this commit) |
| 2026-09-09 | D-174 — full regression | nothing else regressed after the font/image/layout changes (touch every public page) | driven (Vitest 357/357, Playwright 285/286 workers=3, 1 unrelated pre-existing flake green isolated) | n/a — policy | `npx vitest run && npx playwright test --workers=3` | PASS | (this commit) |
| 2026-09-09 | D-175 — #96 real mobile Core Web Vitals baseline, run against production | TTFB/FCP/LCP/CLS/transfer bytes for every priority route, real browser + real mobile profile + real (Lighthouse "Slow 4G" + 4x CPU) throttling | driven (manual run against `https://decaprofesional.es` — a measurement, not a red/green unit) | n/a — new diagnostic script | `npm run perf:baseline -- https://decaprofesional.es` | LCP 1460–1960 ms (well under the 2500 ms "good" threshold) and CLS 0 on every priority route; caught and fixed a real bug in the script itself first (byte counts read from the `content-length` header undercounted to ~0 since most responses are served compressed/chunked with no such header — switched to CDP's `Network.loadingFinished` `encodedDataLength`) | (this commit) |
| 2026-09-09 | D-176 — URGENT: "Reenviar" on a pending invite silently discarded the new link | click "Reenviar" → the invite-link block shows the NEW, working link (not the stale one) → the new link resolves to "Únete al equipo" → the previously-shown link is correctly superseded | driven (Playwright, team.spec.ts extended) | **observed** — `git stash` against the pre-fix `team-manager.tsx` reproduced the exact reported symptom: the displayed link never changed after "Reenviar", so `expect(resentLink).not.toBe(firstLink)` failed exactly as the live report described | `npx playwright test tests/e2e/team.spec.ts --workers=1` | PASS (9/9) | (this commit) |
| 2026-09-09 | D-176 — email-delivery investigation (not a code bug) | a raw call to Resend's API, byte-for-byte identical to the app's own `sendMail()`, sent directly to the affected address | driven (manual, against the real Resend API — not a red/green unit) | n/a — root-cause investigation | `curl https://api.resend.com/emails ...` (same shape as `lib/mailer.ts`) | Resend accepted the send without error (a real message id returned) — rules out an application-code cause; left as a deliverability/domain-configuration question, not guessed at further | n/a — no code changed for this half |
| 2026-09-09 | D-176 — full regression | nothing else regressed | driven (Vitest 357/357, Playwright 287/288 workers=3, 1 skipped by design) | n/a — policy | `npx vitest run && npx playwright test --workers=3` | PASS | (this commit) |
| 2026-09-09 | D-177 — invite-email logging, per the user's explicit correction to D-176 | line-by-line re-read of `lib/mailer.ts` + `app/api/team/invites/route.ts`: `from`/`to`/env reads/`await`/`delivered` derivation all correct as written; the real gaps were a silent success path (no provider id logged) and a silently-swallowed outer catch | driven (re-read + Vitest 357/357 + Playwright team/membership 14/14) | n/a — code-reading investigation, not a red/green unit; the gap itself (missing logs) has no test by nature | `npx vitest run && npx playwright test tests/e2e/team.spec.ts tests/e2e/membership.spec.ts --workers=1` | PASS; `mail_provider_error` log lines confirmed firing correctly even in the test env (no real Resend key there) | (this commit) |
| 2026-09-09 | D-177 — full regression | nothing else regressed | driven (Vitest 357/357, Playwright 287/288 workers=3, 1 documented `master-data.spec.ts` contention flake, green isolated) | n/a — policy | `npx vitest run && npx playwright test --workers=3` | PASS | (this commit) |
| 2026-09-09 | D-178 — #102 follow-up: member of A, joins B (active), removed from B → automatic fallback to A, no onboarding | removal happens while B (not A) is the active company — the automatic fallback itself, not a manual switch, must land on A; Superadmin shows A as active, B's membership gone | driven (Playwright, membership.spec.ts new test + team.spec.ts, 15/15) | n/a — closes a verification gap in an EXISTING passing test (which switched back to A before removal, never exercising the fallback path), not a red-first bug fix — the underlying behavior was already correct when re-read line by line | `npx playwright test tests/e2e/membership.spec.ts tests/e2e/team.spec.ts --workers=1` | PASS (15/15) | (this commit) |
| 2026-09-09 | D-178 — full regression | nothing else regressed | driven (Vitest 357/357) | n/a — policy | `npx vitest run` | PASS | (this commit) |
| 2026-09-09 | #106/D-179/D-180 — shared transactional email shell (pure) | UTF-8 declared explicitly; accented text passed through unescaped; one CTA + the same URL visible as plain text; info block renders company/role/expiry; footer with no unsubscribe language; no `<img>`/external asset; HTML-injection via an untrusted company name neutralised | driven (Vitest, 17/17 across email-template.test.ts + team-invite-email.test.ts) | **observed** — both files failed to collect before `lib/email-template.ts`/`lib/team-invite-email.ts` existed | `npx vitest run tests/unit/email-template.test.ts tests/unit/team-invite-email.test.ts` | PASS (17/17) | (this commit) |
| 2026-09-09 | #106 — real send of the FINAL invite template | the exact HTML/text the app now generates, sent through the same Resend call the app makes, direct to the originally-affected address | driven (manual, against the real Resend API — not a red/green unit) | n/a — verification send, not a regression test | `curl https://api.resend.com/emails ...` | Accepted (message id `ee665537-…`) — pending the user's confirmation of Inbox vs. Spam and correct accent rendering | n/a |
| 2026-09-09 | #106 — full regression after wiring the shared shell into all 8 `sendMail()` call sites | nothing else regressed | driven (Vitest 374/374, Playwright 287/288 workers=3 + 1 skipped) | n/a — policy | `npx vitest run && npx playwright test --workers=3` | PASS; the 1 failure is the already-documented `commercial-intelligence.spec.ts` contention flake (unrelated), green isolated earlier this session | (this commit) |
| 2026-09-09 | #107/D-181 — DeCA PDF structural snapshot after the editorial redesign | every mandatory field (#61 party terminology, #59 postal code/town, #75 no-province) still present verbatim; the old CMR-style numbered-cell adjacency is correctly GONE (rewritten, not deleted, per the deliberate #107 correction); the masthead shows brand/reference/version/status; `DOCUMENTO CORREGIDO` + modification timestamp render for version > 1 | driven (Vitest, deca-pdf-snapshot.test.ts, 9/9) | **observed twice** — the rewritten "numbers the cells" assertion correctly failed against the redesigned component before being rewritten to assert absence; separately, a real render crash (`Cannot read properties of undefined (reading 'unitsPerEm')`) was hit and bisected to a literal `\n` inside a single `<Text>` — fixed with two separate `Text` nodes | `npx vitest run tests/unit/deca-pdf-snapshot.test.ts` | PASS (9/9) | (this commit) |
| 2026-09-09 | #107 — visual QA, 4 real PDFs read directly (before vs. after) | short data; long names/addresses; full trailer + all fields; corrected v2 — read via this tool's own PDF viewer, not just text-extracted | driven (manual, real renders — not a red/green unit, per the issue's own "QA visual" request) | n/a — visual comparison | temporary render script (not committed), PDFs read directly | Confirmed: no dark header, no numbered badges, no dashboard cards, no text cutoff/overlap on long content, corrected-version status/timestamp correct, empty-space problem resolved (proportionate for short data, gone for realistic-length data) | n/a |
| 2026-09-09 | #107 — the "sacred" R-1…R-13 compliance gate, unweakened | native-text PDF ≤5MB, exact public URL, no-auth download, generic 404, creation timestamp, fails closed on invalid payload, served-bytes hash matches stored SHA-256, URL built from the configured base URL | driven (Playwright, compliance.spec.ts, 8/8) | n/a — pre-existing sacred suite, run to confirm the redesign didn't weaken it | `npx playwright test tests/e2e/compliance.spec.ts --workers=1` | PASS (8/8) | (this commit) |
| 2026-09-09 | #107 — full regression | nothing else regressed | driven (Vitest 376/376, Playwright 288/288 workers=3 + 1 skipped) | n/a — policy | `npx vitest run && npx playwright test --workers=3` | PASS — 0 failures this run | (this commit) |
| 2026-09-09 | #107/D-182 — DeCA PDF structural snapshot after the SECOND editorial pass | every mandatory field still present verbatim; the new "Identificación del DeCA" module carries REFERENCIA + the exact reference + VERSIÓN + the version number; DOCUMENTO VIGENTE / DOCUMENTO CORREGIDO + modification timestamp still render correctly | driven (Vitest, deca-pdf-snapshot.test.ts, 9/9) | **observed twice** — `"Identificación del DeCA"` as one literal phrase failed (pdfjs fragments it per-character due to `letterSpacing`, a text-extraction artifact, not a rendering defect — rewritten to assert `"REFERENCIA"` instead); `"Versión 1"`/`"Versión 2"` as one adjacent phrase failed because label and value are now separate fields — rewritten to assert each independently | `npx vitest run tests/unit/deca-pdf-snapshot.test.ts` | PASS (9/9) | (this commit) |
| 2026-09-09 | #107/D-182 — visual QA, same 4-case fixture re-rendered and read directly (D-181 vs. this second pass) | short data; long names/addresses (deliberately extreme); full trailer + all fields; corrected v2 | driven (manual, real renders — not a red/green unit, per the issue's own "no cierres hasta que la mejora sea obvia" request) | n/a — visual comparison | temporary render script (not committed, deleted after each round), PDFs read directly | Confirmed: 5 zones visually distinct via hairlines/tint only (no cards); id-strip fields no longer run together; dashed route divider + origin/destination dots render as intended; goods/vehicle table has real borders on all 4 cells; QR large and uncluttered with the exact requested caption; watermark present, doesn't hurt legibility; short/medium content now fills the page down to the verification band. One real regression found and fixed: the extreme long-names case spilled to a mostly-empty page 2 — trimmed QR/padding/margins; after the fix it still spans 2 pages but degrades cleanly (page 1 fully populated, page 2 normally-styled, not broken-looking); both realistic cases stay single-page | n/a |
| 2026-09-09 | #107/D-182 — the "sacred" R-1…R-13 compliance gate, unweakened | native-text PDF ≤5MB, exact public URL, no-auth download, generic 404, creation timestamp, fails closed on invalid payload, served-bytes hash matches stored SHA-256, URL built from the configured base URL | driven (Playwright, compliance.spec.ts, 8/8) | n/a — pre-existing sacred suite, run to confirm the second pass didn't weaken it | `npx playwright test tests/e2e/compliance.spec.ts --workers=1` | PASS (8/8) | (this commit) |
| 2026-09-09 | #107/D-182 — full regression | nothing else regressed | driven (Vitest 376/376, Playwright 287/288 workers=3 + 1 skipped) | n/a — policy | `npx vitest run && npx playwright test --workers=3` then the 1 failure re-run isolated | PASS; the 1 `--workers=3` failure is the pre-existing `commercial-intelligence.spec.ts:83` contention flake — confirmed green at `--workers=1`, unrelated to this slice | (this commit) |
