# Test Points — Farvertrans DeCA

| Date | Slice / issue | Criterion | Coverage | Red first | Command | Result | Evidence (commit) |
|---|---|---|---|---|---|---|---|
| 2026-09-03 | BUILD 05 scaffold | app boots, `/` renders, one h1, CTA→/crear, no console errors, no 5xx | driven (Playwright smoke, 2/2 pass) | n/a (scaffold) | `npm run test:e2e` | PASS (2 passed, 39.8s) | 7719c- (next commit) |
| 2026-09-03 | BUILD 05 scaffold | env parsing rules (bad URL, short secret, FVD_DEBUG coercion) | driven (Vitest, 3/3 pass) | n/a (mirrors lib/env) | `npm run test:unit` | PASS (3 passed) | 7719c- |
| 2026-09-03 | BUILD 05 scaffold | Prisma schema validates + migrates + seeds | driven | n/a | `npx prisma validate` / `migrate dev` / `npm run seed` | PASS (migration 20260903114315_init applied; 4 operators seeded) | 7719c- |
| 2026-09-03 | BUILD 05 scaffold | lint + typecheck + build + keel-verify pass | driven | n/a | `npm run lint` / `npm run typecheck` / `npm run build` / `node scripts/keel-verify.mjs` | PASS (all clean) | 7719c- |
| 2026-09-03 | BUILD 05 scaffold | health endpoint | driven (Playwright) | n/a | `GET /health` | PASS (200, version + db reported) | 7719c- |
| 2026-09-03 | BUILD 06 landing | AC-25 SSR, one h1 "DeCA GRATIS", CTA→/crear (not contact) | driven (Playwright) | n/a | `npm run test:e2e` | PASS | next commit |
| 2026-09-03 | BUILD 06 landing | AC-26 no pricing/contact/demo/sales gating; 0 `<form>` | driven | n/a | e2e landing.spec | PASS | next commit |
| 2026-09-03 | BUILD 06 landing | AC-27 content + primary CTA work with JS disabled | driven (jsEnabled:false) | n/a | e2e landing.spec | PASS | next commit |
| 2026-09-03 | BUILD 06 landing | AC-32 title/meta/canonical/OG present; robots disallows /api /d/; sitemap public-only | driven | n/a | e2e landing.spec | PASS | next commit |
| 2026-09-03 | BUILD 06 landing | AC-33 JSON-LD SoftwareApplication + FAQPage | driven | n/a | e2e landing.spec | PASS | next commit |
| 2026-09-03 | BUILD 06 landing | AC-31 landing_view event emitted + accepted (204) | driven | n/a | e2e landing.spec | PASS | next commit |
| 2026-09-03 | BUILD 06 landing | AC-29 no serious/critical axe violations on / and /crear; CTA keyboard-reachable | driven (@axe-core/playwright) | n/a | e2e a11y.spec | PASS | next commit |
| 2026-09-03 | BUILD 06 landing | responsive: no horizontal overflow at 360/768/1280; mobile CTA shows <768 only | driven | n/a | e2e landing.spec | PASS | next commit |
| 2026-09-03 | BUILD 06 landing | pickRefSnapshot / eventInputSchema / landing JSON-LD builder | driven (Vitest 7 tests) | pure logic — analytics + content builders | `npm run test:unit` | PASS (10 unit total) | 7262ca1 |
| 2026-09-03 | BUILD 07 creator | validateDeca R-2/AC-09 (missing-field rejection, foreign-NIF warn-not-block, date format, path-keyed errors); normalizePlate; checkNif | driven (Vitest, 10 tests) | logic written from R-2/AC-09, not the impl; tests pass on first run (velocity note — no observed red) | `npm run test:unit` | PASS (20 unit total) | next commit |
| 2026-09-03 | BUILD 07 creator | AC-01 anonymous completes 3 steps → /crear/[id] "DeCA generado" | driven (Playwright) | n/a | e2e crear.spec | PASS | next commit |
| 2026-09-03 | BUILD 07 creator | AC-02 mandatory omission blocks advance, Spanish message, accessible error summary, stays on step | driven | n/a | e2e crear.spec | PASS | next commit |
| 2026-09-03 | BUILD 07 creator | back/forward preserves entered data (sessionStorage draft) | driven | n/a | e2e crear.spec | PASS | next commit |
| 2026-09-03 | BUILD 07 creator | AC-09 POST /api/deca 422 + field errors on missing mandatory field | driven | n/a | e2e crear.spec | PASS | next commit |
| 2026-09-03 | BUILD 07 creator | AC-01 POST /api/deca 201 + ≥256-bit token + claim token for anonymous | driven | n/a | e2e crear.spec | PASS | next commit |
| 2026-09-03 | BUILD 07 creator | AC-04b same idempotency-key never creates a 2nd DeCA | driven | n/a | e2e crear.spec | PASS | next commit |
| 2026-09-03 | BUILD 07 creator | AC-04 wizard axe-clean; progressbar named; error summary role=alert focus | driven (@axe-core) | n/a | e2e a11y.spec + crear.spec | PASS | 7641709 |
| 2026-09-03 | BUILD 08 PDF/QR/URL | R-5/AC-07 QR encodes URL X → jsQR decodes back to exactly X | driven (Vitest, pngjs+jsqr) | pure logic — QR round-trip | `npm run test:unit` | PASS | next commit |
| 2026-09-03 | BUILD 08 PDF/QR/URL | R-8 public token: ≥40 url-safe chars, unique per call | driven (Vitest) | pure logic | `npm run test:unit` | PASS | next commit |
| 2026-09-03 | BUILD 08 PDF/QR/URL | R-9 availability window: available until 7 natural days after service end, then not | driven (Vitest) | pure logic — written from R-9 | `npm run test:unit` | PASS | next commit |
| 2026-09-03 | BUILD 08 PDF/QR/URL | R-3/R-4 PDF is native text (all R-2 values extractable via pdfjs), starts %PDF-, < 5 MB (~20 KB) | driven (Playwright + pdfjs) `test:compliance` | n/a | `npm run test:compliance` | PASS | next commit |
| 2026-09-03 | BUILD 08 PDF/QR/URL | R-5/R-6 PDF carries the exact /d/[token] URL as text | driven | n/a | test:compliance | PASS | next commit |
| 2026-09-03 | BUILD 08 PDF/QR/URL | R-7/R-8 /d/[token] streams the PDF directly: application/pdf, inline, noindex, no set-cookie, not HTML | driven | n/a | test:compliance | PASS | next commit |
| 2026-09-03 | BUILD 08 PDF/QR/URL | R-8 unknown token → generic 404 text/plain | driven | n/a | test:compliance | PASS | next commit |
| 2026-09-03 | BUILD 08 PDF/QR/URL | R-11 PDF metadata records CreationDate + Creator="Farvertrans DeCA v..." | driven (pdfjs getMetadata) | n/a | test:compliance | PASS | next commit |
| 2026-09-03 | BUILD 08 PDF/QR/URL | R-13 fails closed — invalid payload → 422, no document | driven | n/a | test:compliance | PASS | next commit |
| 2026-09-03 | BUILD 08 PDF/QR/URL | standalone build traces lib/pdf/fonts into .next/standalone (Hostinger Docker) | driven | n/a | `NEXT_STANDALONE=1 npm run build` + ls | PASS | 206d734 |
| 2026-09-03 | BUILD 09 signup+claim | password scrypt hash/verify (correct/wrong/malformed); min length | driven (Vitest) | pure logic | `npm run test:unit` | PASS | next commit |
| 2026-09-03 | BUILD 09 signup+claim | session token HMAC sign/verify; tamper + junk rejected | driven (Vitest) | pure logic | `npm run test:unit` | PASS (31 unit total) | next commit |
| 2026-09-03 | BUILD 09 signup+claim | AC-17 generate first → register second → DeCA claimed, appears in /app | driven (Playwright) | n/a | e2e registro.spec | PASS | next commit |
| 2026-09-03 | BUILD 09 signup+claim | auth failure (422) never orphans/deletes the DeCA; claim token still usable after | driven | n/a | e2e registro.spec | PASS | next commit |
| 2026-09-03 | BUILD 09 signup+claim | a claim token cannot be reused by a second account (reports already-used) | driven | n/a | e2e registro.spec | PASS | next commit |
| 2026-09-03 | BUILD 09 signup+claim | signup form carries no lead-qual fields (flota/facturación/empleados/teléfono/demo/cargo) | driven | n/a | e2e registro.spec | PASS | next commit |
| 2026-09-03 | BUILD 09 signup+claim | /registro axe-clean (no serious/critical) | driven (@axe-core) | n/a | e2e a11y.spec | PASS | next commit |
| 2026-09-03 | BUILD 09 signup+claim | /registro axe-clean (no serious/critical) | driven (@axe-core) | n/a | e2e a11y.spec | PASS | fc5609a |
| 2026-09-03 | BUILD 10 workspace | history filter predicate (free text across ref/place/carrier/plate/shipper; date range) | driven (Vitest) | pure logic | `npm run test:unit` | PASS (36 unit) | next commit |
| 2026-09-03 | BUILD 10 workspace | saved-entity schemas (plate normalisation, NIF required) | driven (Vitest) | pure logic | `npm run test:unit` | PASS | next commit |
| 2026-09-03 | BUILD 10 workspace | authed DeCA is owned by the company + appears in /app + /app/historico | driven (Playwright) | n/a | e2e workspace.spec | PASS | next commit |
| 2026-09-03 | BUILD 10 workspace | history search filters (q match / no-match, date range) | driven | n/a | e2e workspace.spec | PASS | next commit |
| 2026-09-03 | BUILD 10 workspace | "Repetir último DeCA" / duplicate prefills, date reset, NEW id/token on generate | driven | n/a | e2e workspace.spec | PASS | next commit |
| 2026-09-03 | BUILD 10 workspace | saved data add → autofill in wizard (company + vehicle) → delete; a generated DeCA is untouched | driven | n/a | e2e workspace.spec | PASS | next commit |
| 2026-09-03 | BUILD 10 workspace | /app, /app/historico, /app/datos axe-clean | driven (@axe-core) | n/a | e2e workspace.spec | PASS | next commit |
| 2026-09-03 | BUILD 11 attribution | parseTouch: referral/campaign/organic/direct classification + all 5 UTMs captured | driven (Vitest) | pure logic — written from F12/EPIC 02 | `npm run test:unit` | PASS (43 unit) | next commit |
| 2026-09-03 | BUILD 11 attribution | merge rules: first-touch captured once + NEVER overwritten after lock; last-touch updates on qualifying touch only; no-op once locked; acquisition-row flatten | driven (Vitest, 5 tests) | pure logic | `npm run test:unit` | PASS | next commit |
| 2026-09-03 | BUILD 11 attribution | AC-18/19 ?ref=adrian → browse → anonymous flow → signup attributed to adrian; user never sees operator name | driven (Playwright) | n/a | e2e attribution.spec | PASS | next commit |
| 2026-09-03 | BUILD 11 attribution | AC-20 return via ?ref=maria before signup → last-touch maria, first stays adrian | driven | n/a | e2e attribution.spec | PASS | next commit |
| 2026-09-03 | BUILD 11 attribution | AC-21/22 no ref + no UTM → organic/direct, still recorded | driven | n/a | e2e attribution.spec | PASS | next commit |
| 2026-09-03 | BUILD 11 attribution | AC-23 per-operator companies/first-DeCA/total-DeCA queryable via /api/operadores/stats (internal only) | driven | n/a | e2e attribution.spec | PASS | next commit |
| 2026-09-03 | BUILD 11 attribution | first_deca_at set when an attributed company generates its first DeCA (authed create + claim) | driven | n/a | e2e attribution.spec | PASS | next commit |
| 2026-09-03 | BUILD 12 dashboard | non-internal user cannot discover /operadores (404) nor /api/operadores/stats (404) — anon + regular user | driven (Playwright) | n/a | e2e operadores.spec | PASS | next commit |
| 2026-09-03 | BUILD 12 dashboard | internal user sees the operator table; per-operator companies/first-DeCA/total-DeCA reconcile with the API | driven | n/a | e2e operadores.spec | PASS | next commit |
| 2026-09-03 | BUILD 13 abuse | decide(): first-time user allowed (no challenge); soft threshold → challenge; hard → block+retry-after | driven (Vitest) | pure logic — F16 | `npm run test:unit` | PASS (47 unit) | next commit |
| 2026-09-03 | BUILD 13 abuse | PoW challenge: valid nonce (plain SHA-256) verified; wrong/forged-prefix/wrong-scope/oversize rejected | driven (Vitest) | pure logic | `npm run test:unit` | PASS | next commit |
| 2026-09-03 | BUILD 13 abuse | AC-34/35 anonymous: first 3 creates never challenged; 4th → 429 challenge; solved PoW retry → 201 | driven (Playwright API) | n/a | e2e build13.spec | PASS | next commit |
| 2026-09-03 | BUILD 13 abuse | AC-36 `GET /d/[token]` never returns 429 (15 rapid fetches all 200) | driven | n/a | e2e build13.spec | PASS | next commit |
| 2026-09-03 | BUILD 13 versioning | AC-14/15 correction → new `deca_version` (new token/QR/PDF), reason + "Versión actual: 2"; v1 PDF still retrievable with original destination; distinct tokens per version | driven (Playwright + pdfjs) | n/a | e2e build13.spec | PASS | next commit |
| 2026-09-03 | BUILD 13 versioning | AC-16 non-owner cannot correct (401 from an unauthenticated context) | driven | n/a | e2e build13.spec | PASS | next commit |
| 2026-09-03 | BUILD 13 sharing | AC-24 share panel: WhatsApp deep link contains the `/d/` URL; email via `POST /api/share` (rate-limited, templated, mailto fallback when unconfigured); `deca_shared` event | driven | n/a | e2e build13.spec | PASS | next commit |
| 2026-09-03 | BUILD 14 SEO | 10 core pages: SSR (JS disabled), exactly one h1, unique <title>, canonical, meta description, FAQPage JSON-LD, CTA→/crear, internal cluster links, last-reviewed date | driven (Playwright, jsEnabled:false) | n/a | e2e seo.spec | PASS | next commit |
| 2026-09-03 | BUILD 14 SEO | an unknown slug 404s (dynamicParams=false) | driven | n/a | e2e seo.spec | PASS | next commit |
| 2026-09-03 | BUILD 14 SEO | sitemap lists all 10 + /soy-obligado, no /app /api /d/; robots disallows private areas | driven | n/a | e2e seo.spec | PASS | next commit |
| 2026-09-03 | BUILD 14 SEO | "¿Estoy obligado?" returns a conclusion with JavaScript disabled (SSR query-param form) | driven | n/a | e2e seo.spec | PASS | next commit |
| 2026-09-03 | BUILD 14 SEO | representative SEO page axe-clean (no serious/critical) | driven (@axe-core) | n/a | e2e seo.spec | PASS | next commit |
| 2026-09-03 | BUILD 15 gate | no cross-tenant access: company B → 404 on A's /app/deca/[id], /corregir, /api/deca/[id]/version; B's history has 0 of A's docs | driven (Playwright, 2 contexts) | n/a | e2e launch-gate.spec | PASS | next commit |
| 2026-09-03 | BUILD 15 gate | security headers on HTML: CSP (default-src 'self', frame-ancestors 'none', no unsafe-eval in prod), nosniff, X-Frame-Options DENY, Referrer-Policy, Permissions-Policy | driven | n/a | e2e launch-gate.spec | PASS | next commit |
| 2026-09-03 | BUILD 15 gate | public tokens ≥240-bit base64url, unique, adjacent guess → 404 | driven | n/a | e2e launch-gate.spec | PASS | next commit |
| 2026-09-03 | BUILD 15 gate | failure paths: malformed create → 422, bad beacon → 204 (never 5xx), unknown token → 404, bad share → 422 | driven | n/a | e2e launch-gate.spec | PASS | next commit |
| 2026-09-03 | BUILD 15 gate | full compliance suite re-run on the build | driven | n/a | `npm run test:compliance` | PASS (6/6 R-1…R-13) | next commit |
| 2026-09-03 | BUILD 15 gate | Docker standalone production build + keel-verify + format:check | driven | n/a | `NEXT_STANDALONE=1 npm run build` / `npm run format:check` | PASS | next commit |
| 2026-09-08 | #85 pt1 WhatsApp channel | commercial channel shows "WhatsApp" not "Teléfono"; `commercialChannelLabelEs()` maps phone→WhatsApp, both→"Correo y WhatsApp"; admin uses it | driven (Vitest) | pure logic — helper | `npm run test:unit tests/unit/commercial-availability.test.ts` | PASS (11) | this commit |
| 2026-09-08 | #85 pt1 WhatsApp channel | /panel/privacidad channel select renders "WhatsApp", never "Teléfono" | driven (Playwright) | n/a | `npx playwright test tests/e2e/commercial-consent.spec.ts` | see run below | this commit |
| 2026-09-08 | #85 pt2 saved-company address | `savedCompanySchema` keeps optional postalCode/city; empty when omitted; rejects >12-char CP | driven (Vitest) | observed — new pure-logic assertions written against the field addition | `npm run test:unit tests/unit/saved-schema.test.ts` | PASS (3) | this commit |
| 2026-09-08 | #85 pt2 saved-company address | picking a saved company in the wizard fills shipper/carrier CP + población into the DeCA party | driven (Playwright) | n/a | `npx playwright test tests/e2e/master-data.spec.ts` | see run below | this commit |
| 2026-09-08 | #85 pt3 launch pricing | landing renders the "Gratis durante 2026" badge; no "gratis para siempre" phrasing; FAQ names the 2027 subscription | driven (Playwright) | n/a | `npx playwright test tests/e2e/landing.spec.ts` | see run below | this commit |
| 2026-09-08 | #85 full slice | typecheck + lint + prettier + full unit suite + keel:verify | driven | n/a | `npx tsc --noEmit` / `npm run lint` / `npx vitest run` / `node scripts/keel-verify.mjs` | PASS (205 unit, lint pre-existing warnings only) | this commit |
| 2026-09-08 | #87 corridors/activity | zoneOf (ES provinces → regional zones, foreign countries, city hints), matchCorridors (Valencia→Lyon = ES↔FR both directions), summariseActivity (7/30/60/90d windows, trend vs prior 30d, weekday histogram) | driven (Vitest) | pure logic — test-first (D-014) | `npm run test:unit tests/unit/commercial-corridors.test.ts tests/unit/commercial-activity.test.ts` | PASS (23) | this commit |
| 2026-09-08 | #87 opportunity model | applyOpportunityFilter (q/state/operator/corridor/destination-across-routes/activity), sortOpportunities (recent/recurrence/volume), deriveOpportunity (movements, nextUnloadDate, contactable) | driven (Vitest) | pure logic | `npm run test:unit tests/unit/commercial-opportunities.test.ts` | PASS (16) | this commit |
| 2026-09-08 | #88 affinity/profile | affinityScore (clamped 0-100, band thresholds, every point a labelled rule, monotonic on adding a contact), autoTags (objective), summariseRoutes (grouping, tallies, 2+ plate, corridor ids) | driven (Vitest) | pure logic — test-first | `npm run test:unit tests/unit/commercial-affinity.test.ts tests/unit/commercial-carrier-profile.test.ts` | PASS (12) | this commit |
| 2026-09-08 | #87/#88 radar + profile e2e | a consented carrier appears on `/admin/oportunidades` with its route activity + WhatsApp action; filter by destination; manual state persists (DB check); `/admin/empresas/[id]` shows the affinity score + breakdown; a NON-consented carrier never appears and has no derived profile; API 404 non-internal, 409 non-eligible | driven (Playwright + Prisma) | n/a | `npx playwright test tests/e2e/commercial-intelligence.spec.ts` | PASS (2) | this commit |
| 2026-09-08 | admin content editor fix (D-153) | create a BLOG draft in `/admin/contenido/nuevo` → lands on the editable detail page with the "Publicar" button → publish → "published" badge + public `/blog/[slug]` 200 | driven (Playwright) | reproduced the user report (soft-nav cache) then fixed with hard navigation | `npx playwright test tests/e2e/content-cms.spec.ts` | PASS (6/6, was a documented flake) | this commit |
| 2026-09-08 | #87+#88 full slice | typecheck + lint + prettier + full unit + production build + full e2e | driven | n/a | `npx tsc --noEmit` / `npm run lint` / `npx vitest run` / `npm run build` / `npx playwright test` | PASS (273 unit; e2e 232 passed / 2 documented flakes green at --workers=1) | this commit |
| 2026-09-08 | #89 KPIs | rollupFunnel (per-state counts, always every state in funnel order), maxProgress (furthest funnel point ever reached, not just current state; terminal-only = -1) | driven (Vitest) | pure logic | `npm run test:unit tests/unit/commercial-kpis.test.ts` | PASS (4) | this commit |
| 2026-09-08 | #90 alert rules | evaluateAlerts — each of the 10 rules fires on its documented condition and NOT otherwise (upcoming priority zone, first corridor, recurrence, growing, uncontacted, stale follow-up, reactivation, referral first DeCA, acquired recurring); quiet carrier → no alerts; dedupeKey stable per company+kind(+week) | driven (Vitest) | pure logic — test-first | `npm run test:unit tests/unit/commercial-alert-rules.test.ts` | PASS (12) | this commit |
| 2026-09-08 | #89/#90 e2e | conversion PATCH writes a whatsapp/converted activity-log entry + the manual outcome + convertedByUserId; `/admin/comercial` shows the funnel + attributed revenue + the activity note; the #88 ficha shows the commercial history; `/admin/alertas-comerciales` surfaces rule-based alerts for a consented carrier and dismissed alerts stay dismissed after a recompute | driven (Playwright + Prisma) | n/a | `npx playwright test tests/e2e/commercial-intelligence.spec.ts` | PASS (2, at --workers=1) | this commit |
| 2026-09-08 | #89+#90 full slice | typecheck + lint + prettier + full unit + production build + full e2e | driven | n/a | `npx tsc --noEmit` / `npm run lint` / `npx vitest run` / `npm run build` / `npx playwright test` | PASS (289 unit; e2e 231 passed / 3 internalPage-contention flakes, all green isolated) | this commit |
| 2026-09-09 | #94 P0 — internal pages closed on every transport | anon / normal customer / internal × 3 RSC transports over all 29 `(protected)` pages + 7 detail routes; plain nav still 404 | driven (Playwright, 6/6) | **observed** — pre-fix run failed on the real leak: `/admin/empresas leaked a real company record to an anonymous caller` and `... leaked its own rendered payload ...`; anon RSC returned 200 with 478 KB / 240 company-name hits (`/admin/activacion` 7.74 MB / 6080) | `npx playwright test tests/e2e/admin-rsc-authz.spec.ts` | PASS (6 passed) | bf3ce0f |
| 2026-09-09 | #94 — the class cannot reopen | every `app/admin/(protected)/**/page.tsx` calls a guard of its own | driven (keel-verify) | n/a — out of scope (a lint check, not product logic) | `node scripts/keel-verify.mjs` | PASS (`all 29 internal pages call requireInternal() themselves (#94)`) | bf3ce0f |
| 2026-09-09 | #92 — saved Histórico views (pure logic) | filter set is exactly the page's own; params→filters; canonical query; active-chip match; name validation; summary | driven (Vitest, 26/26) | **observed** — module absent, suite failed to collect before `lib/data/history-views.ts` existed | `npx vitest run tests/unit/history-views.test.ts` | PASS (26 passed) | (this commit) |
| 2026-09-09 | #93 — quick-access catalogue (pure logic) | catalogue has no duplicate destinations; selection normalised (unknown/repeat/4th dropped); defaults for an unconfigured user; retired key repaired | driven (Vitest, 14/14) | **observed** — module absent, suite failed to collect before `lib/panel/quick-actions.ts` existed | `npx vitest run tests/unit/panel-quick-actions.test.ts` | PASS (14 passed) | (this commit) |
| 2026-09-09 | #92 — saved views end to end | save from active filters, apply in one click, rename, delete, persistence across reload, duplicate-name error shown, private to the user, anonymous 401, another user's id 404 (no IDOR) | driven (Playwright, 5 tests) | n/a — policy (`pure-logic`; the pure half is the two rows above) | `npx playwright test tests/e2e/panel-personalisation.spec.ts` | PASS (9 passed incl. #93) | (this commit) |
| 2026-09-09 | #93 — quick accesses end to end | defaults shown, editable, 4th option disabled at the cap, persists across reload, shortcut navigates, restore defaults, per-user isolation, junk normalised server-side, anonymous 401 | driven (Playwright, 4 tests) | n/a — policy (`pure-logic`) | `npx playwright test tests/e2e/panel-personalisation.spec.ts` | PASS (9 passed incl. #92) | (this commit) |
| 2026-09-09 | #92 + #93 + #94 — full regression | the whole suite, nothing regressed; a11y unchanged on `/panel`, `/panel/historico`, `/panel/datos` | driven (Playwright, 249/249) | n/a — policy | `npm run typecheck && npm run lint && npx vitest run && npx playwright test` | PASS (330 unit, 249 e2e, 0 failed) | (this commit) |
| 2026-09-09 | D-159 — #84 registration opt-in restyled (presentation only) | commercial-opt-in checkbox: unchecked by default, all 8 acceptance cases, withdrawal — unchanged behaviour under the new markup | driven (Playwright, 14/14, existing suite unmodified) | n/a — predates (UI copy/style change over an existing, already-covered control; no new logic) | `npx playwright test tests/e2e/commercial-consent.spec.ts` | PASS (14 passed) | (this commit) |
| 2026-09-09 | #102 — pickFallbackMembership (pure logic) | which remaining membership becomes active when the current one is removed | driven (Vitest, 5/5) | **observed** — module absent, suite failed to collect before `lib/team-membership.ts` existed | `npx vitest run tests/unit/membership-fallback.test.ts` | PASS (5 passed) | (this commit) |
| 2026-09-09 | #102 — exact reported bug reproduced then fixed | invite-accept overwriting an existing company; remove-member orphaning the account; idempotent re-accept; no extra company for a new user; account survives removal | driven (Playwright, 5/5) | n/a — policy (integration flow, not pure logic) | `npx playwright test tests/e2e/membership.spec.ts` | PASS (5 passed) | (this commit) |
| 2026-09-09 | #102 — existing team.spec.ts regression check | invite/accept/role-change/remove flows unaffected by the Membership rewrite | driven (Playwright, 7/7, unmodified except the new confirm dialog) | n/a — policy | `npx playwright test tests/e2e/team.spec.ts` | PASS (7 passed) | (this commit) |
| 2026-09-09 | #102 — full regression | nothing else regressed | driven (Playwright, 254 total) | n/a — policy | `npx playwright test --workers=3` | PASS (253/254; 1 = the documented `master-data.spec.ts:38` contention flake, green isolated) | (this commit) |
| 2026-09-09 | #98 — structured data audit + fix | FAQPage removed (violated the issue's own instruction); WebSite added; Organization taxID/logo added; Article image added when present; escaping normalized | driven (Vitest 336/336, Playwright landing.spec.ts 14/14 + seo-regression.spec.ts 23/23) | **observed** — `landing-content.test.ts` and `landing.spec.ts:48` (AC-33) both failed for the right reason against the pre-fix code (asserted the now-removed FAQPage), then rewritten to assert its absence | `npx vitest run && npx playwright test tests/e2e/landing.spec.ts tests/e2e/seo-regression.spec.ts` | PASS | (this commit) |
| 2026-09-09 | D-170 — #103 correction: no irreversible company action in Superadmin | anonymize/delete/hard_delete all rejected (422) for companies; block→reactivate still works; AccountActions hides the anonymize UI for kind=empresas | driven (Playwright, admin-account-lifecycle.spec.ts 7/7) | **observed** — the pre-existing "anonymise a company" test failed for the right reason once the action was removed (its assertions expected 200/success), then rewritten to assert rejection | `npx playwright test tests/e2e/admin-account-lifecycle.spec.ts` | PASS (7/7) | (this commit) |
| 2026-09-09 | D-171 — #97 internal-linking architecture (pure logic) | hub/strategic slugs are real `content/seo/pages.ts` slugs, no duplicates; `pickCornerstones` never self-links, skips already-linked, deterministic; `suggestRelatedByCategory` matches case/whitespace-insensitively, excludes self, caps at limit; the audit script's `STRATEGIC_ROUTES` copy stays in sync with the lib | driven (Vitest, 11/11) | **observed** — module absent, suite failed to collect before `lib/content/internal-linking.ts` existed | `npx vitest run tests/unit/internal-linking.test.ts` | PASS (11 passed) | (this commit) |
| 2026-09-09 | D-171 — #97 related-content picker end to end | same-category suggestion appears and is addable once (disabled once added); manual filterable pick works independently; both selections survive the save round-trip; the published article's public "Sigue leyendo" block links the selected guide | driven (Playwright, content-cms.spec.ts) | **observed** — `data-testid="ce-related-*"` did not exist before the picker UI was added; `relatedSlugs` was previously write-only (saved, never settable from the UI) | `npx playwright test tests/e2e/content-cms.spec.ts --workers=1` | PASS (6/6) | (this commit) |
| 2026-09-09 | D-171 — #97 internal-links audit script, run against a live build | orphan pages, thin strategic-page inbound linking, broken internal links, repeated-anchor smell, link-heavy pages, click depth from home | driven (manual run against `next start`) | n/a — new diagnostic script, not a red/green unit | `npm run seo:links-audit` | 0 broken links, 0 orphans, 4 expected hub-reinforcement anchor repeats (non-blocking), 2 strategic pages flagged thin for editorial follow-up | (this commit) |
| 2026-09-09 | #97 — full regression | nothing else regressed (347 unit; landing/seo-regression/content-cms e2e suites green; one documented `internalPage`-contention flake on `commercial-intelligence.spec.ts`, green isolated) | driven (Vitest 347/347, Playwright 282/283 parallel + 2/2 isolated) | n/a — policy | `npx vitest run && npx playwright test` | PASS | (this commit) |
| 2026-09-09 | D-172 — #96 CWV: `SiteHeader`'s `locale` prop + `LanguageSwitcher`'s client-side text swap (pure/behavioural) | `header-strings.ts` matches the full dictionaries verbatim in all 8 locales (drift-detection); a static page defaults to Spanish and updates with no navigation on switch; a returning visitor's existing non-Spanish cookie is honoured on load; the still-dynamic landing page is unaffected | driven (Vitest 10/10, Playwright 4/4) | **observed** — `tests/unit/header-strings.test.ts` failed to collect before the file existed; `tests/e2e/i18n-header.spec.ts` exercised `data-testid="header-login"` text before `SiteHeader`'s `locale` prop and the swap logic existed, and it showed the old always-cookie-driven text | `npx vitest run tests/unit/header-strings.test.ts && npx playwright test tests/e2e/i18n-header.spec.ts --workers=1` | PASS (10 unit, 4 e2e) | (this commit) |
| 2026-09-09 | D-172 — #96 CWV: root-cause verification against a real build | confirms the actual, remaining cause of site-wide `Cache-Control: no-store` after the `SiteHeader` fix — `curl` against `npm run build && next start`, with and without an existing `fvd_attr`/locale cookie, and with `middleware.ts` temporarily excluding the test route (ruled out) | driven (manual, against a real production build — not a red/green unit) | n/a — root-cause investigation, not a regression test | `npm run build && npm run start` + `curl -sI` | Confirmed: `app/layout.tsx`'s own `getLocale()` call is the remaining, deeper cause — documented in D-172, not fixed in this slice | (this commit) |
| 2026-09-09 | #96 (D-172 slice) — full regression | nothing else regressed | driven (Vitest 357/357, Playwright — landing/seo-regression/content-cms/i18n-header suites green) | n/a — policy | `npx vitest run && npx playwright test tests/e2e/landing.spec.ts tests/e2e/seo-regression.spec.ts tests/e2e/content-cms.spec.ts tests/e2e/i18n-header.spec.ts --workers=1` | PASS | (this commit) |
| 2026-09-09 | D-173 — URGENT: company-less logged-in user's dead-end loop | removed member, still logged in, visits `/panel` → lands on `/registro/completar-empresa` (not the full signup form), completes a company, reaches a working `/panel` | driven (Playwright, membership.spec.ts extended) | **observed** — `git stash` against the pre-fix `app/panel/**` files reproduced the exact reported loop (`/panel` → `/registro`, dead end) before the fix; green after | `npx playwright test tests/e2e/membership.spec.ts --workers=1` | PASS (5/5) | f018160 |
| 2026-09-09 | D-173 — full regression | nothing else regressed; 2 pre-existing `account.spec.ts` assertions correctly updated to the new, equally-correct final destination (`/entrar`) for a fully invalidated session | driven (Vitest 357/357, Playwright 285/286 workers=3 + isolated re-check) | n/a — policy | `npx vitest run && npx playwright test --workers=3` | PASS | f018160 |
| 2026-09-09 | D-174 — #96 CWV: hero-image CLS fix + Inter font removal + performance budgets | `article-layout.tsx`'s `heroImage` reserves its box before load; Inter fully removed with zero other references left; `scripts/perf-budget.mjs` correctly parses a real build's route table and checks against measured budgets | driven (manual run against a real build; no automated red/green — a diagnostic script, same class as `seo-audit.mjs`) | n/a — new script, not a regression fix | `npm run perf:budget` | 8/8 priority routes within budget (`/` 127 kB, `/crear` 210 kB, `/entrar`+`/registro` 201 kB, `/guias` 128 kB, `/blog`+`/soy-obligado`+`/[slug]` 126 kB) | (this commit) |
| 2026-09-09 | D-174 — full regression | nothing else regressed after the font/image/layout changes (touch every public page) | driven (Vitest 357/357, Playwright 285/286 workers=3, 1 unrelated pre-existing flake green isolated) | n/a — policy | `npx vitest run && npx playwright test --workers=3` | PASS | (this commit) |
| 2026-09-09 | D-175 — #96 real mobile Core Web Vitals baseline, run against production | TTFB/FCP/LCP/CLS/transfer bytes for every priority route, real browser + real mobile profile + real (Lighthouse "Slow 4G" + 4x CPU) throttling | driven (manual run against `https://decaprofesional.es` — a measurement, not a red/green unit) | n/a — new diagnostic script | `npm run perf:baseline -- https://decaprofesional.es` | LCP 1460–1960 ms (well under the 2500 ms "good" threshold) and CLS 0 on every priority route; caught and fixed a real bug in the script itself first (byte counts read from the `content-length` header undercounted to ~0 since most responses are served compressed/chunked with no such header — switched to CDP's `Network.loadingFinished` `encodedDataLength`) | (this commit) |
| 2026-09-09 | D-176 — URGENT: "Reenviar" on a pending invite silently discarded the new link | click "Reenviar" → the invite-link block shows the NEW, working link (not the stale one) → the new link resolves to "Únete al equipo" → the previously-shown link is correctly superseded | driven (Playwright, team.spec.ts extended) | **observed** — `git stash` against the pre-fix `team-manager.tsx` reproduced the exact reported symptom: the displayed link never changed after "Reenviar", so `expect(resentLink).not.toBe(firstLink)` failed exactly as the live report described | `npx playwright test tests/e2e/team.spec.ts --workers=1` | PASS (9/9) | (this commit) |
| 2026-09-09 | D-176 — email-delivery investigation (not a code bug) | a raw call to Resend's API, byte-for-byte identical to the app's own `sendMail()`, sent directly to the affected address | driven (manual, against the real Resend API — not a red/green unit) | n/a — root-cause investigation | `curl https://api.resend.com/emails ...` (same shape as `lib/mailer.ts`) | Resend accepted the send without error (a real message id returned) — rules out an application-code cause; left as a deliverability/domain-configuration question, not guessed at further | n/a — no code changed for this half |
| 2026-09-09 | D-176 — full regression | nothing else regressed | driven (Vitest 357/357, Playwright 287/288 workers=3, 1 skipped by design) | n/a — policy | `npx vitest run && npx playwright test --workers=3` | PASS | (this commit) |
| 2026-09-09 | D-177 — invite-email logging, per the user's explicit correction to D-176 | line-by-line re-read of `lib/mailer.ts` + `app/api/team/invites/route.ts`: `from`/`to`/env reads/`await`/`delivered` derivation all correct as written; the real gaps were a silent success path (no provider id logged) and a silently-swallowed outer catch | driven (re-read + Vitest 357/357 + Playwright team/membership 14/14) | n/a — code-reading investigation, not a red/green unit; the gap itself (missing logs) has no test by nature | `npx vitest run && npx playwright test tests/e2e/team.spec.ts tests/e2e/membership.spec.ts --workers=1` | PASS; `mail_provider_error` log lines confirmed firing correctly even in the test env (no real Resend key there) | (this commit) |
| 2026-09-09 | D-177 — full regression | nothing else regressed | driven (Vitest 357/357, Playwright 287/288 workers=3, 1 documented `master-data.spec.ts` contention flake, green isolated) | n/a — policy | `npx vitest run && npx playwright test --workers=3` | PASS | (this commit) |
| 2026-09-09 | D-178 — #102 follow-up: member of A, joins B (active), removed from B → automatic fallback to A, no onboarding | removal happens while B (not A) is the active company — the automatic fallback itself, not a manual switch, must land on A; Superadmin shows A as active, B's membership gone | driven (Playwright, membership.spec.ts new test + team.spec.ts, 15/15) | n/a — closes a verification gap in an EXISTING passing test (which switched back to A before removal, never exercising the fallback path), not a red-first bug fix — the underlying behavior was already correct when re-read line by line | `npx playwright test tests/e2e/membership.spec.ts tests/e2e/team.spec.ts --workers=1` | PASS (15/15) | (this commit) |
| 2026-09-09 | D-178 — full regression | nothing else regressed | driven (Vitest 357/357) | n/a — policy | `npx vitest run` | PASS | (this commit) |
| 2026-09-09 | #106/D-179/D-180 — shared transactional email shell (pure) | UTF-8 declared explicitly; accented text passed through unescaped; one CTA + the same URL visible as plain text; info block renders company/role/expiry; footer with no unsubscribe language; no `<img>`/external asset; HTML-injection via an untrusted company name neutralised | driven (Vitest, 17/17 across email-template.test.ts + team-invite-email.test.ts) | **observed** — both files failed to collect before `lib/email-template.ts`/`lib/team-invite-email.ts` existed | `npx vitest run tests/unit/email-template.test.ts tests/unit/team-invite-email.test.ts` | PASS (17/17) | (this commit) |
| 2026-09-09 | #106 — real send of the FINAL invite template | the exact HTML/text the app now generates, sent through the same Resend call the app makes, direct to the originally-affected address | driven (manual, against the real Resend API — not a red/green unit) | n/a — verification send, not a regression test | `curl https://api.resend.com/emails ...` | Accepted (message id `ee665537-…`) — pending the user's confirmation of Inbox vs. Spam and correct accent rendering | n/a |
| 2026-09-09 | #106 — full regression after wiring the shared shell into all 8 `sendMail()` call sites | nothing else regressed | driven (Vitest 374/374, Playwright 287/288 workers=3 + 1 skipped) | n/a — policy | `npx vitest run && npx playwright test --workers=3` | PASS; the 1 failure is the already-documented `commercial-intelligence.spec.ts` contention flake (unrelated), green isolated earlier this session | (this commit) |
| 2026-09-09 | #107/D-181 — DeCA PDF structural snapshot after the editorial redesign | every mandatory field (#61 party terminology, #59 postal code/town, #75 no-province) still present verbatim; the old CMR-style numbered-cell adjacency is correctly GONE (rewritten, not deleted, per the deliberate #107 correction); the masthead shows brand/reference/version/status; `DOCUMENTO CORREGIDO` + modification timestamp render for version > 1 | driven (Vitest, deca-pdf-snapshot.test.ts, 9/9) | **observed twice** — the rewritten "numbers the cells" assertion correctly failed against the redesigned component before being rewritten to assert absence; separately, a real render crash (`Cannot read properties of undefined (reading 'unitsPerEm')`) was hit and bisected to a literal `\n` inside a single `<Text>` — fixed with two separate `Text` nodes | `npx vitest run tests/unit/deca-pdf-snapshot.test.ts` | PASS (9/9) | (this commit) |
| 2026-09-09 | #107 — visual QA, 4 real PDFs read directly (before vs. after) | short data; long names/addresses; full trailer + all fields; corrected v2 — read via this tool's own PDF viewer, not just text-extracted | driven (manual, real renders — not a red/green unit, per the issue's own "QA visual" request) | n/a — visual comparison | temporary render script (not committed), PDFs read directly | Confirmed: no dark header, no numbered badges, no dashboard cards, no text cutoff/overlap on long content, corrected-version status/timestamp correct, empty-space problem resolved (proportionate for short data, gone for realistic-length data) | n/a |
| 2026-09-09 | #107 — the "sacred" R-1…R-13 compliance gate, unweakened | native-text PDF ≤5MB, exact public URL, no-auth download, generic 404, creation timestamp, fails closed on invalid payload, served-bytes hash matches stored SHA-256, URL built from the configured base URL | driven (Playwright, compliance.spec.ts, 8/8) | n/a — pre-existing sacred suite, run to confirm the redesign didn't weaken it | `npx playwright test tests/e2e/compliance.spec.ts --workers=1` | PASS (8/8) | (this commit) |
| 2026-09-09 | #107 — full regression | nothing else regressed | driven (Vitest 376/376, Playwright 288/288 workers=3 + 1 skipped) | n/a — policy | `npx vitest run && npx playwright test --workers=3` | PASS — 0 failures this run | (this commit) |
| 2026-09-09 | #107/D-182 — DeCA PDF structural snapshot after the SECOND editorial pass | every mandatory field still present verbatim; the new "Identificación del DeCA" module carries REFERENCIA + the exact reference + VERSIÓN + the version number; DOCUMENTO VIGENTE / DOCUMENTO CORREGIDO + modification timestamp still render correctly | driven (Vitest, deca-pdf-snapshot.test.ts, 9/9) | **observed twice** — `"Identificación del DeCA"` as one literal phrase failed (pdfjs fragments it per-character due to `letterSpacing`, a text-extraction artifact, not a rendering defect — rewritten to assert `"REFERENCIA"` instead); `"Versión 1"`/`"Versión 2"` as one adjacent phrase failed because label and value are now separate fields — rewritten to assert each independently | `npx vitest run tests/unit/deca-pdf-snapshot.test.ts` | PASS (9/9) | (this commit) |
| 2026-09-09 | #107/D-182 — visual QA, same 4-case fixture re-rendered and read directly (D-181 vs. this second pass) | short data; long names/addresses (deliberately extreme); full trailer + all fields; corrected v2 | driven (manual, real renders — not a red/green unit, per the issue's own "no cierres hasta que la mejora sea obvia" request) | n/a — visual comparison | temporary render script (not committed, deleted after each round), PDFs read directly | Confirmed: 5 zones visually distinct via hairlines/tint only (no cards); id-strip fields no longer run together; dashed route divider + origin/destination dots render as intended; goods/vehicle table has real borders on all 4 cells; QR large and uncluttered with the exact requested caption; watermark present, doesn't hurt legibility; short/medium content now fills the page down to the verification band. One real regression found and fixed: the extreme long-names case spilled to a mostly-empty page 2 — trimmed QR/padding/margins; after the fix it still spans 2 pages but degrades cleanly (page 1 fully populated, page 2 normally-styled, not broken-looking); both realistic cases stay single-page | n/a |
| 2026-09-09 | #107/D-182 — the "sacred" R-1…R-13 compliance gate, unweakened | native-text PDF ≤5MB, exact public URL, no-auth download, generic 404, creation timestamp, fails closed on invalid payload, served-bytes hash matches stored SHA-256, URL built from the configured base URL | driven (Playwright, compliance.spec.ts, 8/8) | n/a — pre-existing sacred suite, run to confirm the second pass didn't weaken it | `npx playwright test tests/e2e/compliance.spec.ts --workers=1` | PASS (8/8) | (this commit) |
| 2026-09-09 | #107/D-182 — full regression | nothing else regressed | driven (Vitest 376/376, Playwright 287/288 workers=3 + 1 skipped) | n/a — policy | `npx vitest run && npx playwright test --workers=3` then the 1 failure re-run isolated | PASS; the 1 `--workers=3` failure is the pre-existing `commercial-intelligence.spec.ts:83` contention flake — confirmed green at `--workers=1`, unrelated to this slice | (this commit) |
| 2026-09-09 | #107/D-183 — DeCA PDF structural snapshot after removing the id-strip version field and adding the version footnote | masthead still carries brand/reference/EMITIDO/ESTADO/DOCUMENTO VIGENTE; the version number now appears as "Versión N del documento" in the verification band, not the id strip | driven (Vitest, deca-pdf-snapshot.test.ts, 9/9) | **observed** — the old "VERSIÓN"/bare-digit assertions in the id-strip test correctly failed once the field was removed from the component, before being rewritten to check the new footnote location | `npx vitest run tests/unit/deca-pdf-snapshot.test.ts` | PASS (9/9) | (this commit) |
| 2026-09-09 | #107/D-183 — weight field defaults a bare number to tonnes | "12"→"12 t", "12.5"→"12.5 t", "12,5"→"12,5 t", "1234"→"1234 t"; anything with an existing unit or a genuine alternative measure stays byte-for-byte unchanged (pre-existing VERBATIM test, re-run) | driven (Vitest, deca-validate.test.ts, new test + existing VERBATIM/meaningless-weight tests) | **observed** — the new test would fail against the pre-change schema (no unit appended) | `npx vitest run tests/unit/deca-validate.test.ts` | PASS (18/18, 1 new) | (this commit) |
| 2026-09-09 | #107/D-183 — visual QA, same 4-case fixture re-rendered a third time | short data; long names/addresses; full trailer + all fields; corrected v2 | driven (manual, real renders — not a red/green unit) | n/a — visual comparison, per live user feedback on the D-182 render | temporary render script (not committed, deleted after inspection), PDFs read directly | Confirmed: brand mark (blue square + white check) renders cleanly next to "DeCA Profesional"; identification strip's 3 remaining fields are visually even, no run-together text; version footnote appears small, under the software-version line, exactly as requested; weight shows "12 t" with no manual unit typed | n/a |
| 2026-09-09 | #107/D-183 — the "sacred" R-1…R-13 compliance gate, unweakened | native-text PDF ≤5MB, exact public URL, no-auth download, generic 404, creation timestamp, fails closed on invalid payload, served-bytes hash matches stored SHA-256, URL built from the configured base URL | driven (Playwright, compliance.spec.ts, 8/8) | n/a — pre-existing sacred suite, run to confirm these corrections didn't weaken it | `npx playwright test tests/e2e/compliance.spec.ts --workers=1` | PASS (8/8) | (this commit) |
| 2026-09-09 | #107/D-183 — full regression | nothing else regressed | driven (Vitest 377/377, Playwright 287/288 workers=3 + 1 skipped) | n/a — policy | `npx vitest run && npx playwright test --workers=3` | PASS; the 1 failure is the same pre-existing `commercial-intelligence.spec.ts:83` contention flake documented in D-182, unrelated to this slice | (this commit) |
| 2026-09-09 | #108/D-184 — LIVE BUG: "Marcar como prueba" surfaces a step-up-required error instead of silently doing nothing | `page.route()` forces the real 401 `step_up_required` response on the real PATCH endpoint; clicking the real `mark-test-toggle` button in a genuine logged-in admin session must show "Verifica tu identidad de nuevo para esta acción." + a `/admin/2fa/verify` link | driven (Playwright, admin-account-lifecycle.spec.ts, new test) | **observed** — confirmed FAILING (assertion timed out, nothing appeared) against the unfixed `mark-test.tsx` first, reproducing the user's report exactly, before the fix | `npx playwright test tests/e2e/admin-account-lifecycle.spec.ts --workers=1` | PASS (8/8) | (this commit) |
| 2026-09-09 | #108/D-184 — full regression | nothing else regressed | driven (Vitest 377/377, Playwright 287/288 workers=3 + 1 skipped) | n/a — policy | `npx vitest run && npx playwright test --workers=3` then both failures re-run isolated | PASS; the 2 `--workers=3` failures (`commercial-intelligence.spec.ts:83`, `master-data.spec.ts:38`) are both pre-existing, already-documented `internalPage`-contention flakes — confirmed green together at `--workers=1`, unrelated to this slice | (this commit) |
| 2026-09-09 | #108/D-184 follow-up — the re-verify link returns the admin to the exact ficha, not `/admin` | "Verificar"'s href carries `?next=` pointing at the current path; following it (real or step-up-fresh short-circuit) lands the browser back on `/admin/empresas/[id]` | driven (Playwright, admin-account-lifecycle.spec.ts, `MarkTest` test extended + new `AccountActions` test) | **observed** — `git stash` of just `mark-test.tsx`/`account-actions.tsx` reproduced the exact report (href had no `next`, landed elsewhere) before the fix | `npx playwright test tests/e2e/admin-account-lifecycle.spec.ts --workers=1` | PASS (9/9) | (this commit) |
| 2026-09-09 | #108/D-184 follow-up — full regression | nothing else regressed | driven (Vitest 377/377, Playwright 290 passed/1 skipped workers=3) | n/a — policy | `npx vitest run && npx playwright test --workers=3` | PASS — 0 failures this run (every previously-documented contention flake sat quiet) | (this commit) |
| 2026-09-10 | #111 Sprint A / D-195 — Markdown renderer additions (typed callouts + block images) | `calloutVariant("::: tip")`→"tip", `"::: faq"`→null; `parseImageLine('![a](/guia/x.png "c")')`→{alt,src,caption}; remote/protocol-relative/inline-among-text URLs→null; empty alt allowed; `CALLOUT_LABEL` = Consejo/Importante/Ejemplo | driven (Vitest, tests/unit/markdown-blocks.test.ts, 8 new) | n/a — new pure helpers, no prior behaviour to reproduce (additive syntax) | `npx vitest run tests/unit/markdown-blocks.test.ts` | PASS (8/8) | (this commit) |
| 2026-09-10 | #111 Sprint A / D-195 — the usage guide renders inside Guías like the other guides | listed in `/guias` with the same `ArticleCard`; `/guias/guia-de-uso-deca-profesional` renders h1 + auto TOC with working `#anchor` links; a `::: important` callout shows the "Importante" label; a `<figure>` with `/guia/*` `<img>` + `<figcaption>`; FAQ block renders; indexable (no robots noindex); no horizontal scroll at 320px or 1440px | driven (Playwright, tests/e2e/guia-uso.spec.ts, 3 new) + content-cms.spec.ts (6, unchanged) | n/a — new content + additive rendering, nothing to reproduce | `npx playwright test guia-uso.spec.ts content-cms.spec.ts --workers=1` | PASS (9/9) | (this commit) |
| 2026-09-10 | #111 Sprint A / D-195 — full unit regression | nothing else regressed | driven (Vitest 394/394) | n/a — policy | `npx vitest run` | PASS (394/394, 56 files) | (this commit) |
| 2026-09-10 | #111 Sprint B / D-196 — Help page polish did not change behaviour | `support-tech-channels` + `support-legal-channels` visible; 0 `tel:` links, 1 `mailto:` + 1 `wa.me` in tech, 1 `wa.me` + `info@praetoriaabogados.es` in legal; `ticket-submit` visible; "no garantiza ningún resultado" present; full ticket round-trip (create → admin reply → status → user sees it → user reply) still works | driven (Playwright, panel-help.spec.ts 2 + support-tickets.spec.ts 1, both unmodified) | n/a — visual-only change, existing tests are the regression guard | `npx playwright test panel-help.spec.ts support-tickets.spec.ts a11y.spec.ts panel-nav.spec.ts --workers=2` | PASS (13/13) | (this commit) |
| 2026-09-10 | #111 Sprint C / D-197 — a retried incident submission does not duplicate | same `{category, subject, body}` POSTed to `/api/support` twice in immediate succession → both return `201` with the SAME `{id, number}`; exactly 1 `support_ticket` row + 1 message in the DB; a different subject the same second is NOT deduped (new row) | driven (Playwright, support-tickets.spec.ts, new test) | n/a — new guard, additive (no prior duplicate-prevention behaviour to reproduce); the notification fires 1:1 with `supportTicket.create` so 1 row ⇒ 1 email | `npx playwright test support-tickets.spec.ts --workers=1` | PASS (2/2) | (this commit) |
| 2026-09-10 | #111 Sprint D / D-198 — API/ERP shows "Próximamente" AND "+ coste adicional" | on `/#planes` the Business tier's 3 `soon` features (API, ERP/TMS, onboarding) each render the "Próximamente" pill AND a "+ coste adicional" marker (count 3 each); the section text contains "no están incluidas en el precio de la suscripción"; no "€80" anywhere; no 320px horizontal scroll | driven (Playwright, plans.spec.ts, extended) | n/a — additive commercial copy, no prior behaviour to reproduce | `npx playwright test plans.spec.ts landing.spec.ts --workers=2` | PASS (all) | (this commit) |
| 2026-09-10 | #111 Sprint D / D-198 — integration request reaches a destination + no duplicate | `POST /api/integraciones` persists an `IntegrationRequest`, it appears in `/admin/integraciones`, triage works; the same payload POSTed twice in immediate succession → both `200`, exactly 1 row (⇒ 1 notification email, fired 1:1 with the insert); a different `system` the same second → a new row | driven (Playwright, admin-growth.spec.ts, existing flow + new dedup test) | n/a — the notification + dedup are new/additive | `npx playwright test admin-growth.spec.ts --workers=1` | PASS (4/4) | (this commit) |
| 2026-09-10 | D-201 — "DECA Conecta" rename preserves all consent behaviour | registration checkbox unchecked by default; accepting Terms/Privacy alone → mode `none`; the 3 stored modes (`none`/`per_deca`/`all`) + per-DeCA override + channel + revocation + "record carries only the 4 authorised fields, no excluded data" all unchanged; the article link `/blog/deca-conecta-ofertas-carga` is used; no "Oportunidades de carga"/"Kilómetro Cero" as a name in the cards; both cards no h-scroll at 375px, no raw i18n keys | driven (Playwright, commercial-consent.spec.ts — 5 new + 18 existing) | n/a — rename is UI/copy only, the 18 existing tests ARE the behaviour-preservation guard (one assertion updated: the disclosure summary text) | `npx playwright test commercial-consent.spec.ts --workers=1` | PASS (23/23) | (this commit) |
| 2026-09-10 | D-201 — admin surface heading renamed without breaking the RSC-authz guard | `/admin/tratamiento-comercial` heading is now "DECA Conecta"; the route path is unchanged; the SECURITY #94 per-transport authz assertions still hold | driven (Playwright, admin-rsc-authz.spec.ts) | n/a — label-only change | `npx playwright test admin-rsc-authz.spec.ts --workers=1` | PASS (19/19) | (this commit) |
| 2026-09-11 | I-112/D-205 Sprint 1 — schema helpers: default/override resolution, backward-compat mirror, weight sum | `resolveShipment` inherits DeCA-level defaults unless a shipment overrides them; `decaPayloadSchema` accepts BOTH the flat legacy body and the new `shipments[]` body; a flat body normalizes to a 1-element `shipments` array; empty `shipments` rejected; a shipment's own resolved unload date before its resolved load date rejected even when the DeCA-level dates alone are fine; `legacyMirrorFields` mirrors shipment 1 only; `sumWeights` sums numeric weights to kg, never fabricates a total when any shipment's weight is non-numeric | driven (Vitest, `tests/unit/deca-schema-shipments.test.ts`, 13 new) | n/a — new pure helpers, no prior behaviour to reproduce (additive) | `npx vitest run tests/unit/deca-schema-shipments.test.ts` | PASS (13/13) | (this commit) |
| 2026-09-11 | I-112/D-205 Sprint 1 — PDF renders every shipment as a separated, numbered block | 2 shipments (shared shipper/carrier, one overriding the DeCA-level date) → both "ENVÍO N" badges, both routes/goods/weights, a PESO TOTAL line, the ordering disclaimer; every pre-existing 1-shipment snapshot/compliance assertion passes UNMODIFIED (byte-identical layout for the still-default case) | driven (Vitest `deca-pdf-snapshot.test.ts` +1 new case; Playwright `compliance.spec.ts` re-run unmodified) | n/a — additive rendering path, gated by `shipments.length > 1` | `npx vitest run tests/unit/deca-pdf-snapshot.test.ts && npx playwright test compliance.spec.ts --workers=1` | PASS (10/10 unit incl. new case; 8/8 compliance unmodified) | (this commit) |
| 2026-09-11 | I-112/D-205 Sprint 1 — LIVE finding: `legacyMirrorFields` written but never wired into the persist write path | the cockpit's "Datos del documento" (`doc-cockpit.spec.ts`), a saved template (`creator-v2.spec.ts`), a correction (`build13.spec.ts`), and the field-error path for a flat-body 422 (`crear.spec.ts`) all read/behaved correctly once `dataJson` actually carries the flat mirror (`toDataJson()` wired into `createDeca`/`correctDeca`) | driven (Playwright, 5 pre-existing specs) | **observed** — all 5 FAILED first (cockpit showed empty loadLocation/goods/weight for a plain single-shipment DeCA; field-error path key mismatch), confirming the helper was unreachable before the fix; see `docs/lessons-learned.md` 2026-09-11 | `npx playwright test crear.spec.ts creator-v2.spec.ts creator-ux31.spec.ts compliance.spec.ts build13.spec.ts doc-cockpit.spec.ts reliability.spec.ts` | PASS (all) after `toDataJson()` wired in | (this commit) |
| 2026-09-11 | I-112/D-205 Sprint 1 — real browser, real server: the issue's own Valencia→Madrid + Castellón→Madrid example | toggling "¿Varios lugares…?" off by default (single-shipment flow pixel-identical, toggle unchecked, generation unaffected); adding a second envío, generating, downloading the REAL PDF via `/d/[token]` and text-extracting it → both ENVÍO blocks, both routes/goods, PESO TOTAL (20.000 kg), the disclaimer, shipper/carrier each appearing exactly once; removing the only extra shipment turns the toggle back off | driven (Playwright, `tests/e2e/deca-multi-shipment.spec.ts`, 3 new) | n/a — new feature, additive; the "byte-identical single-shipment" claim IS the regression guard (asserted directly, not just implied) | `npx playwright test deca-multi-shipment.spec.ts` | PASS (3/3) | (this commit) |
| 2026-09-11 | I-112/D-205 Sprint 1 — the LIVE, real caller `recordAvailabilityShare` (#84) also assumed the flat shape | `app/api/deca/route.ts` passed `validated.data` (not the mirrored `dataJson`) straight into `recordAvailabilityShare`, same class of bug as `legacyMirrorFields` above but a SECOND, independent instance the first sweep missed — a grep for every direct `validated.data` consumer found it. Fixed by resolving shipment 1 at the call site (`resolveShipments(validated.data)[0]`), same "first shipment is the summary" choice used elsewhere | driven (Playwright, `commercial-consent.spec.ts`, 5 pre-existing tests) | **observed** — all 5 FAILED both before AND immediately after the first attempt at this fix, which traced to a SEPARATE bug: an orphaned `node.exe` from an earlier OOM-killed run was still bound to port 3000, so `reuseExistingServer` silently kept testing yesterday's build (`docs/lessons-learned.md` 2026-09-11 "stale server" entry) — killing that process and forcing a fresh build made the real fix visible | `npx playwright test commercial-consent.spec.ts --workers=1` | PASS (23/23) after both the code fix and the stale-server kill | (this commit) |
| 2026-09-11 | I-112/D-205 Sprint 1 — exhaustive sweep for any other direct consumer of `validated.data` (not the stored/mirrored `dataJson`) | grepped every `.loadLocation`/`.unloadLocation` site across `app/`, `lib/`, `components/`; confirmed each one reads from stored `dataJson` (safe, carries the mirror) or an unrelated "saved template" record, except the two already found and fixed (`route-intel.ts`, `recordAvailabilityShare`) | driven (manual code sweep, not automatable — the check IS the grep itself) | n/a — verification sweep, not a reproduction | `grep -rn "\.loadLocation\b\|\.unloadLocation\b" app lib components` (manually traced each hit's data origin) | Every remaining site confirmed safe | (this commit) |
| 2026-09-11 | I-112/D-205 Sprint 1 — full regression | nothing else regressed | driven (Vitest 413/413; full Playwright suite, 2 complete runs after both fixes + the stale-server kill) | n/a — policy | `npx vitest run && npx playwright test --workers=2` | PASS — 321/323 + 1 skipped in the combined run; the 1 failure (`content-cms.spec.ts`) is a pre-existing, already-documented contention flake, confirmed 6/6 green in isolation at `--workers=1`, unrelated to this slice | (this commit) |
| 2026-09-11 | I-112/D-206 Sprint 2 — `toDisplayDeca` never recursed into `shipments[]`, breaking #86p3's UPPERCASE guarantee for every shipment beyond the first | a lower-case shipment 2 (`loadLocation.name`, `goods`, `recipient`, `notes`) → the function's OUTPUT is uppercase, same as shipment 1 | driven (Vitest, `deca-display.test.ts`, 2 new cases) | **observed** — `'almacén norte' to be 'ALMACÉN NORTE'` before the fix | `npx vitest run tests/unit/deca-display.test.ts` | PASS (6/6) after adding the `shipments[]` recursion | (this commit) |
| 2026-09-11 | I-112/D-206 Sprint 2 — the PDF snapshot test's own #112 case was too weak to have caught the bug above | shipment 2's fields checked against the RAW (non-uppercased) extracted text specifically, not just the already-uppercased comparison used for presence checks elsewhere in this file | driven (Vitest, `deca-pdf-snapshot.test.ts`, assertions added to the existing #112 case) | **observed** — would have failed against the pre-D-206 `toDisplayDeca` (confirmed by re-deriving the assertion from the bug just fixed, not by literally reverting and re-running) | `npx vitest run tests/unit/deca-pdf-snapshot.test.ts` | PASS (10/10) | (this commit) |
| 2026-09-11 | I-112/D-206 Sprint 2 — `diffVersions` shipment-awareness: added/removed/edited shipment, shipment-1 recipient | added shipment 2 → one "Envío 2" row naming the route; removed → the inverse; a field changed within shipment 2 → "Envío 2 — <campo>", never confused with shipment 1's own fields; shipment 1's `recipient` change → its own row (the one field never covered by the existing flat-mirror diff) | driven (Vitest, `deca-diff.test.ts`, 5 new cases in a `#112` nested describe) | n/a — additive extension of an existing pure function, the 4 pre-existing tests (unmodified) are the regression guard that the flat-field diff stayed untouched | `npx vitest run tests/unit/deca-diff.test.ts` | PASS (9/9) | (this commit) |
| 2026-09-11 | I-112/D-206 Sprint 2 — CSV export gains `envios_totales` without disturbing existing columns | the exact header row, column-for-column, with the new column landing after `mercancia` | driven (Vitest, `deca-export.test.ts`, existing header assertion updated) | n/a — additive column, the existing assertion IS the regression guard (it would have failed had the new column landed anywhere else or renamed an existing one) | `npx vitest run tests/unit/deca-export.test.ts` | PASS (6/6) | (this commit) |
| 2026-09-11 | I-112/D-206 Sprint 2 — real browser: the review summary actually shows shipment 2 before generating, and the Historial table shows the "+1 envío" badge | the same Valencia→Madrid + Castellón→Madrid 2-shipment creation flow, extended: the review section (before clicking Generar) contains "Envío 2" and its real field values, not just shipment 1's; after generating, `/panel/historico`'s row for this DeCA contains "+1 envío" | driven (Playwright, `deca-multi-shipment.spec.ts`, existing test extended) | n/a — additive UI, the strengthened assertion itself is what proves item #1 above isn't a no-op (it would have passed even with the review-summary change entirely unbuilt, before being strengthened) | `npx playwright test deca-multi-shipment.spec.ts --workers=1` | PASS (4/4) | (this commit) |
| 2026-09-11 | I-112/D-206 Sprint 2 — real browser: correcting an already-multi-shipment DeCA pre-loads its extra envíos and the correction's diff names them | create a 2-shipment DeCA → open `/panel/deca/[id]/corregir` → the multi-shipment toggle is ALREADY checked and shipment 2's fields are ALREADY filled with no manual re-entry → edit shipment 2's weight → save with a correction reason → the resulting "qué ha cambiado" view contains "Envío 2" and the new weight | driven (Playwright, `deca-multi-shipment.spec.ts`, 1 new test) | n/a — new capability, additive; the pre-fill assertions themselves are the proof (this exact flow would 404/blank-fill without the `WizardInitial.extraShipments` wiring) | `npx playwright test deca-multi-shipment.spec.ts --workers=1 -g "Sprint 2"` | PASS (1/1) | (this commit) |
| 2026-09-11 | I-112/D-206 Sprint 2 — full regression | nothing else regressed, including every Sprint 1 guarantee (single-shipment flow byte-identical, backward-compat mirror, both real bugs from Sprint 1 stay fixed) | driven (Vitest 420/420; full Playwright suite, 324 e2e tests) | n/a — policy | `npx vitest run && npx playwright test --workers=2` | PASS — 321/324 + 1 skipped; the 2 failures (`admin-2fa.spec.ts` recovery-code-replay, `content-cms.spec.ts` preview) are both unrelated to #112 (2FA/CMS, no file either touches), confirmed 19/19 green together in isolation at `--workers=1` — `content-cms.spec.ts` matches this project's already-documented contention-flake pattern; `admin-2fa.spec.ts` is a first-seen instance of the same pattern | (this commit) |
| 2026-09-11 | I-113/D-207 Phase 1 — `savedShipmentSchema` validation | minimal shape (only 2 location ids) accepted; `goods`/`recipient` uppercased (#86p3), `weight` verbatim; missing `loadLocationId`/`unloadLocationId` each rejected; `name` kept verbatim (never uppercased — a free label, unlike every other saved field) | driven (Vitest, `saved-shipment-schema.test.ts`, 5 new) | n/a — new pure schema, no prior behaviour to reproduce (additive) | `npx vitest run tests/unit/saved-shipment-schema.test.ts` | PASS (5/5) | (this commit) |
| 2026-09-11 | I-113/D-207 Phase 1 — `templatePayloadSchema.shipments` backward-compat + validation | a legacy template with no `shipments` key stays valid (`undefined`, not `[]` — no regression); a template with 1+ valid shipments parses; an inner shipment missing a required field (e.g. `goods` too short) is rejected; an explicit empty array is accepted | driven (Vitest, `template-shipments.test.ts`, 4 new) | n/a — additive optional field, the legacy-template case IS the regression guard | `npx vitest run tests/unit/template-shipments.test.ts` | PASS (4/4) | (this commit) |
| 2026-09-11 | I-113/D-207 Phase 1 — real browser: picking a saved route fills shipment 1 and produces a correct PDF | 2 saved places + 1 saved shipment created via the real API → in `/crear`, the "Usar ruta/envío habitual" picker fills load/unload location + goods/weight in one action (values UPPERCASE, per #86p3 — SavedLocation's own storage rule) → generate → the real downloaded PDF contains both place names and the goods, text-extracted | driven (Playwright, `saved-shipments.spec.ts`, new) | **observed** — the test's own first draft asserted the wrong (lowercase) casing for the filled fields and picked an unreachable option label before the wizard advanced to the right step; both were test bugs, not product bugs, confirmed by querying the dev DB directly with the Prisma client before fixing the assertions | `npx playwright test tests/e2e/saved-shipments.spec.ts` | PASS (2/2) after fixing the test's own assertions | (this commit) |
| 2026-09-11 | I-113/D-207 Phase 1 — real browser: "guardar como envío habitual" round-trips | fill shipment 1 from existing saved places (not a saved route) → "☆ Guardar como envío habitual" → confirm → success message → fresh `/crear` visit → the just-saved route is now selectable by its own name in the picker | driven (Playwright, `saved-shipments.spec.ts`, new) | n/a — new capability, additive; the round-trip itself (create → reload → reselect) is the proof, not just the create call succeeding | `npx playwright test tests/e2e/saved-shipments.spec.ts` | PASS (2/2, same run as above) | (this commit) |
| 2026-09-11 | I-113/D-207 Phase 1 — targeted regression sweep (not the full suite; scoped to every surface this slice touched) | the pre-existing single/multi-shipment wizard flows, the anonymous 3-step creator, the template save/reuse flow (proving the `templates.ts` schema-file split + `shipments` extension caused no regression), and favouriting a saved record all still work unmodified | driven (Playwright: `deca-multi-shipment.spec.ts`, `crear.spec.ts`, `creator-v2.spec.ts`, `favorites.spec.ts`) | n/a — regression guard, not a reproduction | `npx playwright test tests/e2e/deca-multi-shipment.spec.ts tests/e2e/crear.spec.ts tests/e2e/creator-v2.spec.ts tests/e2e/favorites.spec.ts` | PASS (19/19) | (this commit) |
| 2026-09-11 | I-113/D-207 Phase 1 — full unit regression + static gates | nothing else regressed | driven (Vitest 429/429, 59 files) | n/a — policy | `npx vitest run tests/unit && npx tsc --noEmit && npx eslint <changed files> && npx prettier --check <changed files>` | PASS (429/429; tsc/eslint/prettier all clean) | (this commit) |
| 2026-09-11 | I-113/D-208 Phase 2 — `saved-dedup.ts` per-kind duplicate detection | a real duplicate (same NIF/plate/address/load+unload pair) caught across case/format variations; a genuinely different value, a reversed load/unload pair, and a pair sharing only one leg never flagged | driven (Vitest, `saved-dedup.test.ts`, 10 new) | **observed** — `Cannot find module '@/lib/data/saved-dedup'` before the file existed | `npx vitest run tests/unit/saved-dedup.test.ts` | PASS (10/10) | (this commit) |
| 2026-09-11 | I-113/D-208 Phase 2 — real browser: Rutas tab needs 2 places first, then create/favorite/edit/delete a saved route through the real UI (not the API) | the tab's create action is absent until 2 saved locations exist; once they do, creating via the two `<select>`s works, the new route is favoritable/editable/deletable, and appears in the wizard's own "usar ruta habitual" picker | driven (Playwright, `datos-habituales-rutas.spec.ts`, new) | n/a — new UI over an already-tested API (Phase 1), additive | `npx playwright test tests/e2e/datos-habituales-rutas.spec.ts` | PASS (4/4) | (this commit) |
| 2026-09-11 | I-113/D-208 Phase 2 — global search filters the active tab's list | 2 saved vehicles, search "Norte" → only the matching one shows; clearing the search restores both | driven (Playwright, `datos-habituales-rutas.spec.ts`) | n/a — new capability, additive | `npx playwright test tests/e2e/datos-habituales-rutas.spec.ts` | PASS (same run) | (this commit) |
| 2026-09-11 | I-113/D-208 Phase 2 — §13 duplicate warning is non-blocking | saving a vehicle with a plate that differs only in format/case from an existing one shows the duplicate warning; "Guardar de todas formas" still saves it (2 records, not silently merged or rejected) | driven (Playwright, `datos-habituales-rutas.spec.ts`) | n/a — new capability, additive | `npx playwright test tests/e2e/datos-habituales-rutas.spec.ts` | PASS (same run) | (this commit) |
| 2026-09-11 | I-113/D-208 Phase 2 — LIVE bug: `/panel/datos#rutas` deep-link didn't open the Rutas tab | visiting the URL directly shows `tab-shipment`'s `aria-selected="true"` and the Rutas empty state, not the Empresas tab; the dashboard's new "Rutas habituales" quick action is offered in the Personalizar list | driven (Playwright, `datos-habituales-rutas.spec.ts`, new) | **observed** — failed with `aria-selected="false"` before the `useState`→`useEffect` hydration fix | `npx playwright test tests/e2e/datos-habituales-rutas.spec.ts` | PASS (same run) after the fix | (this commit) |
| 2026-09-11 | I-113/D-208 Phase 2 — a11y: the redesigned tabs don't introduce a violation | `/panel/datos`'s existing axe scan (`workspace.spec.ts`) stays clean at wcag2a/wcag2aa/wcag22aa, serious/critical | driven (Playwright, `workspace.spec.ts`, pre-existing test unmodified) | **observed indirectly** — the conditional-mount version would have failed `aria-valid-attr-value` (dangling `aria-controls`) had it shipped; caught by reasoning about the pattern before running, then confirmed clean after switching to the always-mounted/`hidden` approach | `npx playwright test tests/e2e/workspace.spec.ts -g a11y --workers=1` | PASS | (this commit) |
| 2026-09-11 | I-113/D-208 Phase 2 — existing master-data/favorites/workspace specs updated to the new tab/modal selectors, same outcomes | saved-record creation, edit-in-place, deletion, dropdown autofill in `/crear`, and the "materially faster duplicate" flow all still work through the redesigned UI | driven (Playwright, `master-data.spec.ts` 2/2, `favorites.spec.ts` 1/1, `workspace.spec.ts` 7/7) | n/a — regression guard through a changed DOM path, not a reproduction | `npx playwright test tests/e2e/master-data.spec.ts tests/e2e/favorites.spec.ts tests/e2e/workspace.spec.ts` | PASS (10/10) | (this commit) |
| 2026-09-11 | I-113/D-208 Phase 2 — full regression sweep across every surface D-207/D-208 touched | nothing else regressed, including #112's multi-shipment flows and the anonymous/authenticated creator | driven (Playwright: `saved-shipments.spec.ts` 2/2, `deca-multi-shipment.spec.ts` 4/4, `crear.spec.ts` 9/9, `creator-v2.spec.ts` 5/5) | n/a — policy | `npx playwright test tests/e2e/saved-shipments.spec.ts tests/e2e/deca-multi-shipment.spec.ts tests/e2e/crear.spec.ts tests/e2e/creator-v2.spec.ts` | PASS (20/20; 1 transient `ECONNRESET` on an earlier combined run confirmed a contention flake, 4/4 green in isolation) | (this commit) |
| 2026-09-11 | I-113/D-208 Phase 2 — full unit regression + static gates | nothing else regressed | driven (Vitest 439/439, 60 files) | n/a — policy | `npx vitest run tests/unit && npx tsc --noEmit && npx eslint <changed files> && npx prettier --check <changed files> && node scripts/keel-verify.mjs` | PASS (439/439; tsc/eslint/prettier/keel-verify all clean) | (this commit) |
| 2026-09-11 | Production migration-gap check before I-114 | production DB matches `prisma/migrations/` exactly except for D-207's `saved_shipment` (expected — not yet applied); after applying, zero gap | driven (`prisma migrate status`/`deploy` against a transient production connection string, deleted after use) | n/a — verification, not a reproduction | `npx prisma migrate status` then `npx prisma migrate deploy` against production | PASS — 1 pending migration found and applied; `relrowsecurity: true` + 0 rows confirmed on the new table via a direct query | (this commit) |
| 2026-09-11 | I-114/D-209 — `extraRouteSummaries()` multi-envío route summary | no shipments / single shipment → `[]`; skips shipment 1, summarises the rest as "ORIGEN → DESTINO"; caps at 3 regardless of how many extra shipments exist; prefers a place's `name` over `city` matching `formatLocationShort`'s own convention | driven (Vitest, `history-routes.test.ts`, 4 new) | n/a — new pure helper, no prior behaviour to reproduce (additive) | `npx vitest run tests/unit/history-routes.test.ts` | PASS (4/4) | (this commit) |
| 2026-09-11 | I-114/D-209 — real browser: a multi-envío DeCA shows its extra route + badge in the redesigned row | the row for a 2-shipment DeCA contains "+1 envío" and the second shipment's load-location name | driven (Playwright, `historico-redesign.spec.ts`, new) | n/a — new UI over already-existing data (`shipmentCount` since D-206), additive | `npx playwright test tests/e2e/historico-redesign.spec.ts` | PASS (4/4) | (this commit) |
| 2026-09-11 | I-114/D-209 — real browser: the "···" `RowMenu` opens/closes via mouse AND keyboard, exposing Corregir/Duplicar/PDF | click opens it and shows all 3 menu items; Escape closes it and returns focus to the trigger button (a11y); focusing the trigger and pressing Enter also opens it | driven (Playwright, `historico-redesign.spec.ts`) | n/a — new component, additive; the focus-return assertion specifically proves the keyboard path, not just that a click works | `npx playwright test tests/e2e/historico-redesign.spec.ts` | PASS (same run) | (this commit) |
| 2026-09-11 | I-114/D-209 — LIVE bug: a route-level `loading.tsx` broke the "Limpiar filtros" link | clicking "Limpiar filtros" from the filtered-empty-state must actually navigate to `/panel/historico` (no query) and the previously-hidden row must reappear | driven (Playwright, `historico-redesign.spec.ts`, new) | **observed** — failed with the URL still `?plate=0000ZZZ` after the click (root cause isolated via a temporary request-logging debug spec, deleted after diagnosis: the navigation's own RSC fetch fired then self-aborted); fixed by REMOVING `app/panel/historico/loading.tsx` entirely, confirmed via a controlled A/B (only that file toggled) | `npx playwright test tests/e2e/historico-redesign.spec.ts` | PASS (4/4) after removing `loading.tsx` | (this commit) |
| 2026-09-11 | I-114/D-209 — real browser: mobile cards, no horizontal scroll | at 375px, `historico-cards` is visible and `historico-table` is hidden; the card shows the "+1 envío" badge, a "Ver detalle" link, and the `RowMenu` still exposes Corregir; `document.documentElement.scrollWidth - clientWidth` ≤ 1px | driven (Playwright, `historico-redesign.spec.ts`) | n/a — new UI, additive; the overflow measurement is the actual proof, not just a visual read | `npx playwright test tests/e2e/historico-redesign.spec.ts` | PASS (same run) | (this commit) |
| 2026-09-11 | I-114/D-209 — filter logic genuinely untouched: 7 pre-existing specs re-run unmodified | every pre-existing action (detail/inspección/compartir/corregir/duplicar/PDF, saved-data autofill flowing into a row, CSV export, team-scoped history visibility, the a11y scan of `/panel/historico`) still passes through the redesigned DOM with the SAME assertions, no locator rewrites needed beyond the "Detalle"→"Ver detalle" substring-match design already confirmed | driven (Playwright: `workspace.spec.ts` 7/7, `creator-v2.spec.ts` 5/5, `driver-delivery.spec.ts` 3/3, `team.spec.ts` 9/9, `master-data.spec.ts` 2/2, `export-csv.spec.ts` 3/3, `row-share.spec.ts` 1/1) | n/a — regression guard through a changed DOM path, not a reproduction | `npx playwright test tests/e2e/workspace.spec.ts tests/e2e/creator-v2.spec.ts tests/e2e/driver-delivery.spec.ts tests/e2e/team.spec.ts tests/e2e/master-data.spec.ts tests/e2e/export-csv.spec.ts tests/e2e/row-share.spec.ts` | PASS (30/30) | (this commit) |
| 2026-09-11 | I-114/D-209 — full unit regression + static gates | nothing else regressed | driven (Vitest 443/443, 61 files) | n/a — policy | `npx vitest run tests/unit && npx tsc --noEmit && npx eslint <changed files> && npx prettier --check <changed files> && node scripts/keel-verify.mjs` | PASS (443/443; tsc/eslint/prettier/keel-verify all clean) | (this commit) |
| 2026-09-11 | I-115/D-210 — version bump is in sync everywhere it's checked mechanically | `package.json.version === lib/version.ts APP_VERSION`; `/health` reports the new version against a real running build | driven (`node scripts/keel-verify.mjs`'s existing version-sync check; `curl /health` against a real `npm run build && npm run start`) | n/a — mechanical check, not a reproduction | `node scripts/keel-verify.mjs && curl http://localhost:3000/health` | PASS — "version in sync (0.3.0)"; `{"status":"ok","version":"0.3.0","db":"up"}` | (this commit) |
| 2026-09-11 | I-115/D-210 — LIVE finding: editing the guide's TS body does not update an already-seeded `ContentItem` row | after a targeted `contentItem.update` (temporary script, deleted after use), the SAME row id, `published` status, and total `ContentItem` count (5→5) are preserved — no duplicate, no reseed, matching the exact D-201c precedent for this row | driven (manual Prisma query before/after, via `npx tsx` against the dev DB) | **observed** — confirmed `seedContent()` is create-only-if-absent by reading the source, then confirmed a plain `npm run seed:content` re-run would have silently no-opped (not executed, to avoid an unnecessary destructive-adjacent step; reasoned from the guarded `if (existing) continue`) | direct Prisma `contentItem.update` + before/after `contentItem.count()` | PASS — id/status/count unchanged, body updated | (this commit) |
| 2026-09-11 | I-115/D-210 — the "Varios envíos" guide section is documented and reachable from the TOC | the heading appears in the auto TOC, its anchor (`#varios-envios-en-un-mismo-deca`) resolves and scrolls into view, and the section's own body (the "Añadir otro envío" instruction, the Castellón→Madrid worked example) is present | driven (Playwright, `guia-uso.spec.ts`, 1 new test) | n/a — new content, additive; the anchor-id assertion was derived from `slugifyHeading()`'s real algorithm, not guessed, then confirmed by running the test | `npx playwright test tests/e2e/guia-uso.spec.ts` | PASS (4/4) | (this commit) |
| 2026-09-11 | I-115/D-210 — real screenshots regenerated against a genuine production build match the redesigned UI | `mis-deca.png` (Historial) and `datos-habituales.png` regenerate automatically with #113/#114's new UI via the SAME existing script calls (no script change needed for those two); the new `crear-multi-envio.png` shows the full ENVÍO 2 block | driven (manual: `node scripts/guide-screenshots.mjs` against a real `npm run build && npm run start`, PNGs read/inspected directly) | **observed** — the first `crear-multi-envio.png` capture had a real tool bug (the sticky header baked into the middle of a too-tall element screenshot); fixed by measuring the block's real height, widening the viewport to fit it, and compensating the scroll position for the header's own height; re-captured and visually confirmed clean | `node scripts/guide-screenshots.mjs` (twice — before/after the capture fix) | PASS — all 12 PNGs regenerated, `crear-multi-envio.png` clean on the second pass | (this commit) |
| 2026-09-11 | I-115/D-210 — guide e2e suite, run twice (once per this session's own webServer lesson) | all 4 guide tests pass whether the server happens to already be running or is started fresh by Playwright's own `webServer` | driven (Playwright, `guia-uso.spec.ts`, full file both times) | n/a — regression + environment-discipline guard, not a reproduction | `npx playwright test tests/e2e/guia-uso.spec.ts` (twice, server state varied deliberately) | PASS (4/4 both times) | (this commit) |
| 2026-09-11 | I-115/D-210 — the one `admin.spec.ts` test touching the updated `appVersion` fixture | the generation-failure detail page still renders and the triage flow (note + resolve) still works with the fixture's version string updated to match the real bump | driven (Playwright, `admin.spec.ts`, existing test, unmodified assertions) | n/a — the fixture value is never asserted against `APP_VERSION` directly; updated for consistency, verified it still passes | `npx playwright test tests/e2e/admin.spec.ts -g "correlation code and can be triaged"` | PASS (1/1) | (this commit) |
| 2026-09-11 | I-115/D-210 — full unit regression + static gates | nothing else regressed (this issue changed content/config/scripts, not logic — no new unit tests expected or added) | driven (Vitest 443/443, 61 files) | n/a — policy | `npx vitest run tests/unit && npx tsc --noEmit && npx eslint <changed files> && npx prettier --check <changed files> && node scripts/keel-verify.mjs` | PASS (443/443; tsc/eslint/prettier/keel-verify all clean) | (this commit) |
| 2026-09-11 | I-116/D-211 — `pt.ts` structural fidelity to `es.ts` (`satisfies Messages`) | every key, array length, object shape, and function signature in the new Portuguese dictionary matches the Spanish source exactly — a missing/extra key fails the build, not a silent blank string | driven (`npx tsc --noEmit` against the whole repo with `pt.ts` wired into `server.ts`/`client.tsx`) | n/a — the type system itself is the check, not a reproduction | `npx tsc --noEmit` | PASS — clean, no errors | (this commit) |
| 2026-09-11 | I-116/D-211 — `header-strings.ts` stays byte-exact with the full dictionaries for the new 9th locale | `tests/unit/header-strings.test.ts`'s existing `it.each(LOCALES)` parameterised check now also covers `pt`, comparing every header string verbatim | driven (Vitest, `header-strings.test.ts`, existing test extended by the locale list growing — no new test written, `pt` added to `FULL_DICTS`) | **observed** — a real discrepancy WAS caught this way: the hand-written `header-strings.ts` `pt.regulation` ("Regulamento") didn't match `pt.ts`'s own `nav.regulation` ("Regulamentação") until fixed; this is exactly the kind of drift the test exists to catch | `npx vitest run tests/unit/header-strings.test.ts` | PASS (11/11) after the fix | (this commit) |
| 2026-09-11 | I-116/D-211 — real browser: Portuguese is selectable, updates the header live, and a full dynamic-page load renders real translated content | switching to `pt` via `language-switcher-pt` updates `header-login` to "Entrar" and the switcher's own display to "pt" with no navigation; a fresh load of `/` (dynamic, server-rendered) with the `pt` cookie set shows the real hero h1 in Portuguese | driven (Playwright, `i18n-header.spec.ts`, 1 new test) | n/a — new locale, additive; the full-page-load half of the test specifically proves coverage beyond the header slice (the issue's own "no fallback-to-Spanish gaps" AC), not just the client-patched header text | `npx playwright test tests/e2e/i18n-header.spec.ts` | PASS (4/4, 1 pre-existing unrelated `test.fixme` skip) | (this commit) |
| 2026-09-11 | I-116/D-211 — the 9th locale doesn't break the switcher's fixed-footprint dropdown (D-081) | the switcher menu stays anchored on-screen and the page shows no horizontal overflow at 360/768/1280px with one more locale in the list | driven (Playwright, `landing.spec.ts`, existing test re-run unmodified) | n/a — regression guard confirming a prior redesign's own claim ("footprint constant regardless of locale count") still holds, not assumed | `npx playwright test tests/e2e/landing.spec.ts` | PASS (17/18, 1 pre-existing unrelated `test.fixme` skip) | (this commit) |
| 2026-09-11 | I-116/D-211 — full unit regression + static gates | nothing else regressed | driven (Vitest 444/444, 61 files) | n/a — policy | `npx vitest run tests/unit && npx tsc --noEmit && npx eslint <changed files> && npx prettier --check <changed files> && node scripts/keel-verify.mjs` | PASS (444/444; tsc/eslint/prettier/keel-verify all clean) | (this commit) |
| 2026-09-12 | I-117/D-212 — Historial row: status/plate/actions vertically centered against wrapped multi-line content | a DeCA with a deliberately long shipper/carrier name (route + shipper + carrier cells all wrapping onto 3+ lines) shows the "Vigente" badge, plate and action links visually centered against the row's full height, not pinned to the top; a short-name row (nothing to wrap) is visually unchanged | driven (manual: temporary uncommitted Playwright script against a real production build, screenshot inspected directly, then deleted) | **observed** — the "before" behavior (everything pinned to the top of a tall row) was confirmed by reading the code (`<tr>`'s `align-top` cascading to every unstyled `<td>`) before changing it, then the "after" screenshot confirmed the fix visually | temporary script (deleted after use) → `.screenshot()` on `[data-testid='historico-table']` | PASS — visually confirmed centered in the long-name row, unchanged in the short-name row | (this commit) |
| 2026-09-12 | I-117/D-212 — targeted e2e regression sweep (CSS-only change; confirms no functional/mobile regression) | every existing Historial action, the mobile card layout (a flex-column with no `<table>`/`vertical-align` involved), the a11y scan, and CSV export/row-share all still work unmodified | driven (Playwright: `historico-redesign.spec.ts` 4/4, `workspace.spec.ts` 7/7 incl. a11y, `master-data.spec.ts` 2/2, `export-csv.spec.ts` 1/1, `row-share.spec.ts` 1/1) | n/a — regression guard for a CSS-only change, not a reproduction | `npx playwright test tests/e2e/historico-redesign.spec.ts tests/e2e/workspace.spec.ts tests/e2e/master-data.spec.ts tests/e2e/export-csv.spec.ts tests/e2e/row-share.spec.ts` | PASS (17/17) | (this commit) |
| 2026-09-12 | I-117/D-212 — full unit regression + static gates | nothing else regressed (CSS-only change, no logic touched — no new unit tests expected or added) | driven (Vitest 444/444, 61 files) | n/a — policy | `npx vitest run tests/unit && npx tsc --noEmit && npx eslint <changed files> && npx prettier --check <changed files>` | PASS (444/444; tsc/eslint/prettier all clean) | (this commit) |
