# API Index — Farvertrans DeCA
> One line per public surface. Grep here FIRST; open the full doc only on a hit.

| Surface | Kind | Code file | Doc | Purpose (one line) |
|---------|------|-----------|-----|--------------------|
| GET /health | route | app/health/route.ts | docs/reference/endpoints.md | Liveness + DB reachability + app version |
| POST /api/events | route | app/api/events/route.ts | docs/reference/endpoints.md | First-party analytics ingest; schema-validated, always 204, never 5xx |
| POST /api/deca | route | app/api/deca/route.ts | docs/reference/endpoints.md | Create a DeCA (F1): requires an authenticated, email-verified, non-`read_only` session (401 `auth_required` / 403 `email_not_verified` / 403 `forbidden`, D-052/D-053/PRODUCT #56) with a complete company ficha (409 `company_data_incomplete`, #59); validate R-2, render compliant PDF, store it, persist deca+version, bump "last used" on any saved records referenced, return token + pdfSha256; 422 on invalid, fails closed. Accepts EITHER the flat single-shipment body (unchanged) or `{ ..., shipments: [...] }` for several loading/unloading legs sharing one shipper/carrier (#112, D-205) |
| GET /api/search | route | app/api/search/route.ts | docs/reference/endpoints.md | Company workspace global search (PRODUCT #56 command palette): authenticated, company-scoped, read-only (available to `read_only` members too); backs `Ctrl+K` |
| GET /d/[token] | route | app/d/[token]/route.ts | docs/reference/endpoints.md | Public inspection (F4/R-6..R-9): streams the exact PDF, no auth/cookie/interstitial, noindex; 404 unknown, 410 outside the 7-day window. The 404 path (unknown/malformed token) is rate-limited via `checkAbuse("d_404", ...)` — 429 at the hard tier, never a CAPTCHA (#125); a successful fetch is never checked |
| renderDecaPdf() / PdfRenderError | fn/class | lib/pdf/render.ts | docs/reference/lib.md | Render the compliant text PDF (R-3), embed QR (R-5) + metadata timestamps (R-11), enforce ≤5 MB (R-4); accepts `customerLogoDataUri` (PRODUCT #39/#49), baked into that render only |
| DecaDocument | component | lib/pdf/deca-document.tsx | docs/reference/lib.md | The premium @react-pdf document (PRODUCT #49) — navy header with brand mark + optional customer logo + status pill, two-column party/route cards, labeled goods/vehicle grid, footer with QR + verification URL; every field stays real selectable text. #112: one route+goods/vehicle block per resolved shipment; exactly 1 shipment renders identically to before #112 (no badge, no total); 2+ get a solid-fill "ENVÍO N" badge per shipment, a PESO TOTAL line (only when every weight is numeric-parseable), and a fixed disclaimer that the numbering is identificative, not an execution order |
| detectLogoImage() / validateLogoUpload() / LogoValidationError | fn/class | lib/company/logo.ts | docs/reference/lib.md | Pure PNG/JPEG detection from real magic bytes + dimensions (never trusts a claimed MIME type), size + max-dimension validation (PRODUCT #39) |
| POST /api/company/logo | route | app/api/company/logo/route.ts | docs/reference/endpoints.md | Upload the company's optional PDF header logo (owner-only); validates the decoded bytes, stores a canonical data URI; only new documents are affected |
| DELETE /api/company/logo | route | app/api/company/logo/route.ts | docs/reference/endpoints.md | Remove the company logo (owner-only); historical PDFs are never touched |
| POST /api/company/consent | route | app/api/company/consent/route.ts | docs/reference/endpoints.md | Set the company's commercial-treatment preference (#84, owner-only): `{mode}` (none/per_deca/all) \| `{channel,contactEmail?,contactPhone?}` \| `{action:"revoke"}`; writes a `commercial_consent_event`; returns `{treatment}` |
| POST /api/deca/[id]/availability | route | app/api/deca/[id]/availability/route.ts | docs/reference/endpoints.md | Withdraw or edit (`{action:"update"}`, #119) the commercial availability record prepared for a DeCA (#84, owner-only); never deletes it, never touches the DeCA; 404 if none pending. `update` accepts the 2026-correction fields (`availabilityPostalCode`/`preferredDestinationPostalCode`/`vehicleTypeOther`) and the 6-value `vehicleType` enum |
| commercial-treatment lib | module | lib/consent.ts + lib/commercial/{types,availability}.ts | docs/flows/commercial-consent.md | `getCommercialTreatment` / `setCommercialMode` / `setCommercialChannel` / `revokeCommercial` / `recordCommercialEvent`; `buildAvailabilityPayload` (pure — the one place that decides what a porte shares) / `recordAvailabilityShare` / `getAvailabilityShare` / `withdrawAvailabilityShare` / `updateAvailabilityShare`; `sharedFieldKeys` (client-safe). `recordAvailabilityShare`'s `DecaFacts` stays a narrow, deliberately minimal type (unchanged); its caller (`app/api/deca/route.ts`) now resolves shipment 1 (`resolveShipments()[0]`) before constructing it, since #112 moved `unloadLocation`/`unloadDate` off the top level of the validated payload (D-205). `VehicleType` (`lib/commercial/types.ts`) is a 6-value union (`lona`/`frigorifico`/`megatrailer`/`jumbo`/`frigolona`/`otro`) + `isVehicleType()` guard; `findCompatibleAvailabilities()`'s matching now prefers postal code (`postalCodesMatch()` — exact or same 2-digit province prefix) over free-text locality whenever both sides of a comparison have one (2026 correction to #119, D-230) |
| qrPngDataUri() / qrPngBuffer() | function | lib/pdf/qr.ts | docs/reference/lib.md | QR PNG for a URL (error correction H) |
| ensureFonts() | function | lib/pdf/fonts.ts | docs/reference/lib.md | Register bundled Inter (OFL) as data URIs for @react-pdf |
| getPdfStore() / pdfKey() / pdfSha256() / PdfStore / StorageError | fn/type | lib/storage/index.ts | docs/reference/lib.md | Pluggable PDF store: Supabase Storage (prod) or local FS (FVD_STORAGE_DIR persistent path); pdfSha256() is the per-version integrity anchor |
| isPubliclyAvailable() | function | lib/deca/deactivation.ts | docs/reference/lib.md | R-9 7-day post-service availability window |
| POST /api/auth/register | route | app/api/auth/register/route.ts | docs/reference/endpoints.md | Create company + user (DB-unique on email, D-203 — a concurrent duplicate request gets a clean 409 `email_taken`, never a raw 500), set session, claim a legacy anonymous DeCA link if present (never orphans it). `emailSent` reflects whether mail is CONFIGURED (sync, no I/O) — the actual send runs after the response via `after()` (D-204), not confirmed delivery; the verify-email screen's "Reenviar" button is the safety net. Every step is timed and logged (`register_timing` / `register_background_timing`). A self-registering company must give a complete, well-formed ficha (#59): name, CIF/NIF (Spanish checksummed, or a plausible foreign tax id, D-202), contact person, phone, email, address, postal code (Spanish or plausible foreign, D-202), town — 409 `bad_input` otherwise |
| POST /api/auth/complete-company | route | app/api/auth/complete-company/route.ts | docs/reference/endpoints.md | Step 2 of Google sign-up: attach a company to a company-less account; same full-ficha requirement as register (#59) |
| PATCH /api/company/profile | route | app/api/company/profile/route.ts | docs/reference/endpoints.md | Owner-only edit of the company contact ficha (email/phone/address/postal code/town/contact person; name+nif stay locked, #62). Each field format-validated (#59); a complete valid submission stamps `dataCompletedAt` and lifts the soft gate — response `complete: boolean` |
| companyDataSchema / COMPANY_DATA_FIELDS | schema | lib/validation/company.ts | docs/reference/lib.md | The one zod schema for a complete company ficha (#59), shared by the register / complete-company / profile routes |
| isValidSpanishPostalCode() / isValidPostalCode() / isValidPhone() / isValidOwnNif() | function | lib/validation/spanish.ts | docs/reference/lib.md | Field validators for "our own company" forms (#59). `isValidSpanishPostalCode` is the strict ES-only 5-digit rule; `isValidPostalCode` (used by the schema/soft-gate) tries that first and falls back to a lenient foreign-postal-code check. `isValidOwnNif` checksums a recognisably Spanish DNI/NIE/CIF strictly, and accepts any other digit-bearing 5–20 char value as a plausible foreign tax id — live-incident fix: a Portuguese company's NIPC/postal code was previously hard-rejected at self-registration (D-202) |
| companyDataComplete() / missingCompanyFields() | function | lib/company/completeness.ts | docs/reference/lib.md | Soft-gate check (#59): whether a company row carries every mandatory ficha field, well-formed |
| PATCH /api/admin/empresas/[id] | route | app/api/admin/empresas/[id]/route.ts | docs/reference/endpoints.md | Superadmin company lifecycle (#62/#103): `edit` (incl. name/nif), `block`/`deactivate`/`reactivate`, `set_test` (#103). **No irreversible action** — `anonymize` and any hard delete are deliberately absent (D-170): 422 if requested. `getInternalUser` → 404, `requireStepUp` → 401 `step_up_required`, audited |
| PATCH /api/admin/usuarios/[id] | route | app/api/admin/usuarios/[id]/route.ts | docs/reference/endpoints.md | Superadmin user lifecycle (#62): `block`/`deactivate`/`reactivate`/`anonymize`. Same gating + audit |
| setUserStatus() / setCompanyStatus() / setCompanyTest() / LifecycleError | fn/class | lib/admin/lifecycle.ts | docs/reference/lib.md | #62 status transitions: write status, kill affected sessions on block/deactivate, audit. `setCompanyTest()` (#103) — the "Marcar como prueba" toggle, visibility/metrics only, never access or data |
| anonymizeUser() / anonymizeCompany() / ANONYMIZED_*_FIELDS | fn/const | lib/admin/anonymize.ts | docs/reference/lib.md | #62 irreversible in-place PII overwrite; never deletes a row / DeCA / audit entry (D-067). `anonymizeCompany()` is NOT wired to any web endpoint (D-170, #103) — reserved for a future controlled, out-of-band procedure only; `anonymizeUser()` is unaffected, still reachable via `PATCH /api/admin/usuarios/[id]` |
| getUserAdmin() | function | lib/admin/records.ts | docs/reference/lib.md | Cross-tenant user detail for `/admin/usuarios/[id]` (#62) — status, company, audit rows |
| DECA_ROLES | constant | lib/deca/roles.ts | docs/reference/lib.md | Canonical Spanish names for the two DeCA parties ("cargador contractual" / "transportista efectivo", #61) for the PDF / diff / zod surfaces; UI copy uses `t.legal.roles.*` |
| toDisplayDeca() | function | lib/deca/display.ts | docs/reference/lib.md | Presentation-only UPPERCASE normalisation of every visible textual field (#86 p3) — PDF, cockpit, history, admin all read through this before display; stored `dataJson` keeps original casing. Recurses into `shipments[]` (#112, D-206) so a shipment beyond the first (which lives ONLY in that array, never mirrored to the top level) gets the identical treatment — a real gap found and fixed this session: it previously rendered shipment 1 uppercase and every later shipment in whatever casing the operator typed |
| techSupportChannels() / legalAssistanceChannel() / whatsappLink() | function | lib/support/channels.ts | docs/reference/lib.md | The panel help-centre channels (#63); WhatsApp omitted until its number is set in `lib/brand.ts` |
| POST /api/support · POST /api/support/[id]/reply | route | app/api/support/ | docs/reference/endpoints.md | Open a technical-support ticket / reply on your own ticket (#86 p5); authed non-`read_only`, rate-limited (`share` policy), 422 with per-field errors. An identical ticket (same user+company+category+subject) within 2 min is de-duped — returns the existing `{id, number}`, no second notification email (#111) |
| PATCH /api/admin/support/[id] | route | app/api/admin/support/[id]/route.ts | docs/reference/endpoints.md | Superadmin reply and/or status change on a ticket (#86 p5); `isInternalRequest` → 404 otherwise; a reply emails the user + sets `awaiting_user` |
| lib/support/tickets.ts (createSupportTicket / listUserTickets / getUserTicket / addUserReply / listSupportTickets / getSupportTicket / updateSupportTicket) | module | lib/support/tickets.ts | docs/reference/lib.md | Technical-support ticket store (#86 p5) — company-scoped for the user side, full inbox for the superadmin; every reply recorded + best-effort emailed (notification to `FVD_SUPPORT_NOTIFY_EMAIL \|\| BRAND.supportEmail`, carries number/company+id/user+id/category/date/body/admin link). `createSupportTicket` de-dupes an identical ticket within 2 min (#111). Legal queries never enter here (#86 p6) |
| lib/support/schema.ts (createTicketSchema / ticketReplySchema / adminTicketUpdateSchema / SUPPORT_STATUSES / SUPPORT_CATEGORIES + labels) | module | lib/support/schema.ts | docs/reference/lib.md | Support-ticket zod + the 5 states (Nueva/En revisión/Pendiente de usuario/Resuelta/Cerrada) and categories (#86 p5) |
| GET /api/auth/verify-email/status | route | app/api/auth/verify-email/status/route.ts | docs/reference/endpoints.md | Fresh, uncached read of `emailVerifiedAt` for the current session (D-053) — backs "Ya he confirmado mi cuenta", which never marks the account verified itself |
| POST /api/auth/verify-email/resend | route | app/api/auth/verify-email/resend/route.ts | docs/reference/endpoints.md | Rotates the verification token (invalidates the previous one) and resends; rate-limited via the "auth" abuse policy; `delivery` in the response is `sent`\|`unconfigured`\|`error`\|`already_verified` — the real outcome, never assumed success |
| POST /api/auth/verify-email/change-email | route | app/api/auth/verify-email/change-email/route.ts | docs/reference/endpoints.md | Changes the pending email + resends verification to the new address |
| POST /api/auth/login | route | app/api/auth/login/route.ts | docs/reference/endpoints.md | Email + password login; optional claim on the way in |
| signup() / login() / getCurrentUser() / AuthError | fn/class | lib/auth/index.ts | docs/reference/lib.md | v1 own auth (D-021); minimal signup, SSR session |
| hashPassword() / verifyPassword() / isStrongEnough() | function | lib/auth/password.ts | docs/reference/lib.md | scrypt password hashing (constant-time verify) |
| signSession() / verifySession() / SESSION_COOKIE | fn/const | lib/auth/session.ts | docs/reference/lib.md | HMAC-signed 30-day session token + cookie options |
| safeInternalPath() | function | lib/auth/safe-redirect.ts | docs/reference/lib.md | Post-auth `next` redirect safety (#38): only a same-origin path, never an absolute/protocol-relative URL or a bounce back into an auth screen |
| contentInputSchema / contentWarnings() / ContentInput | module | lib/content/schema.ts | docs/reference/lib.md | CMS content shape + client-safe editorial warnings (#32) — heuristics, not a score |
| createContent() / updateContent() / setStatus() / resolvePublic() / listPublished() / listPublishedFull() / SlugTakenError | module | lib/content/cms.ts | docs/reference/lib.md | CMS DB layer (#32): guides/blog, slug-change redirects via previousSlugs, soft archive |
| listPublishedFullSafe() / resolvePublicSafe() / resolveRelatedSafe() | function | lib/content/cms.ts | docs/reference/lib.md | Defensive wrappers (D-041) — a DB/config failure logs and degrades to an empty result, never an unhandled exception; used by /guias, /blog and their [slug] routes |
| estimateReadingMinutes() | function | lib/content/reading-time.ts | docs/reference/lib.md | Pure ~200 wpm estimate for the ArticleCard reading-time badge |
| SEO_HUBS / STRATEGIC_ROUTES / pickCornerstones() / suggestRelatedByCategory() | module | lib/content/internal-linking.ts | docs/reference/lib.md | #97 — internal-linking architecture: the issue's example hubs/strategic pages grounded to real routes; hub-link picking and same-category CMS suggestions, both pure |
| HEADER_STRINGS / HEADER_APP_NAME | const | lib/i18n/header-strings.ts | docs/reference/lib.md | #96 (D-172) — client-safe slice of the header's translated strings (9 locales as of #116/D-211), kept in sync with the full dictionaries by a dedicated test; used by `SiteHeader`'s static-locale path and `LanguageSwitcher`'s client-side text swap |
| Markdown / extractHeadings() / slugifyHeading() / calloutVariant() / parseImageLine() | module | lib/content/markdown.tsx (+ markdown-toc.ts) | docs/reference/lib.md | Safe in-house Markdown→React renderer for CMS bodies (#32) — no dangerouslySetInnerHTML; headings/lists/links/blockquote/tables/FAQ/[[cta]], plus (#111) typed callouts `::: tip\|important\|example` and block images `![alt](/local.png "pie")` (local paths only) |
| GET/PATCH/DELETE /api/admin/contenido[/[id]] | route | app/api/admin/contenido/ | docs/reference/endpoints.md | CMS create / update+status / soft-archive (#32); internal session or x-fvd-admin-token, 404 otherwise |
| claimDeca() / ClaimError | fn/class | lib/deca/claim.ts | docs/reference/lib.md | Attach an anonymous DeCA to a company via its one-time claim token (D-016) |
| GET /api/saved | route | app/api/saved/route.ts | docs/reference/endpoints.md | The COMPANY's (WORKSPACE #24, D-052 scope change from per-user) saved companies/vehicles/locations, sorted by last used (wizard autofill dropdowns) |
| POST /api/saved/[kind] | route | app/api/saved/[kind]/route.ts | docs/reference/endpoints.md | Create a saved company (role + mandatory postalCode/city, #86 p2)/vehicle (with alias)/location (structured, load/unload/both), scoped to the company, any non-`read_only` member (PRODUCT #56); 422 with per-field errors on invalid |
| DELETE·PATCH /api/saved/[kind]/[id] | route | app/api/saved/[kind]/[id]/route.ts | docs/reference/endpoints.md | Delete or edit-in-place a saved entity (company-scoped, any non-`read_only` member, PRODUCT #56); PATCH keeps id/relations/favourite, 422 with per-field errors on invalid (#86 p1); never touches generated DeCA |
| listSaved() / createSaved() / updateSaved() / deleteSaved() / touchSavedUsage() | function | lib/data/saved.ts | docs/reference/lib.md | Saved-entity CRUD, company-scoped; updateSaved edits in place with the same schema as create (#86 p1); touchSavedUsage bumps `lastUsedAt` on whichever records populated a generated DeCA |
| savedCompany/Vehicle/LocationSchema / savedShipmentSchema / savedKinds | const | lib/data/saved-schema.ts | docs/reference/lib.md | zod schemas for saved entities (plate normalisation; savedCompany requires postalCode + city — #85 added them, #86 p2 made them mandatory). `savedShipmentSchema` (#113) — a reusable single leg, always by `loadLocationId`/`unloadLocationId` referencing two existing `SavedLocation` rows, never a free-text address |
| GET/POST /api/saved-shipments | route | app/api/saved-shipments/route.ts | docs/reference/endpoints.md | #113 — list/create "rutas/envíos habituales" for the company; create verifies both location ids belong to the caller's company (422 `bad_location` otherwise) |
| DELETE·PATCH /api/saved-shipments/[id] | route | app/api/saved-shipments/[id]/route.ts | docs/reference/endpoints.md | #113 — delete or edit-in-place a saved shipment (company-scoped, any non-`read_only` member); never touches a generated DeCA |
| listSavedShipments() / createSavedShipment() / updateSavedShipment() / deleteSavedShipment() / setSavedShipmentFavorite() / touchSavedShipmentUsage() / SavedLocationOwnershipError | function/class | lib/data/saved-shipments.ts | docs/reference/lib.md | #113 — "ruta/envío habitual" CRUD, company-scoped, mirrors lib/data/saved.ts; create/update reject (`SavedLocationOwnershipError`) a location id from another company |
| POST /api/favorites | route | app/api/favorites/route.ts | docs/reference/endpoints.md | Toggle a company-scoped favourite (#78) on a saved company/vehicle/location/shipment (#113), a template, or a route corridor; discriminated by `kind`, authorised against the session's companyId |
| listHistory() / listHistoryCarriers() / getDecaForDuplicate() | function | lib/data/history.ts | docs/reference/lib.md | Company-scoped DeCA history (rows carry shipper/carrier/plates/versionNo) + distinct carriers for the filter + the duplicate-flow source payload. `HistoryRow.shipmentCount` (#112, D-206) — always 1 pre-#112 and for the still-common single-shipment case; every route/goods field on the row still describes shipment 1 only. `HistoryRow.extraRoutes` (#114, D-209) — up to 3 route summaries for shipments beyond the first, for the redesigned row's multi-envío line |
| extraRouteSummaries() | function | lib/data/history-routes.ts | docs/reference/lib.md | #114, D-209 — pure: up to `cap` (default 3) "ORIGEN → DESTINO" summaries for shipments beyond the first. Split out from history.ts (server-only) so it stays unit-testable, mirroring the pre-existing history-filter.ts split |
| RowMenu | component | components/deca/row-menu.tsx | docs/reference/lib.md | #114 §4/§13, D-209 — the Historial row's "···" overflow menu (Corregir/Duplicar/PDF); mirrors RowShare's popover mechanics, adds Escape-to-close + focus-return to the trigger |
| searchCompanyDecas() | function | lib/data/search.ts | docs/reference/lib.md | Company-scoped global search (PRODUCT #56) — reuses `listHistory`'s `q` free-text filter, top 8 hits with a label/sub/href shape for the command palette. `label` appends a plain-Spanish "(+N envíos)" suffix when `shipmentCount > 1` (#112, D-206 — this file has no locale dictionary threaded through, unlike full pages) |
| getTopRoutes() | function | lib/data/route-intel.ts | docs/reference/lib.md | Company-scoped route intelligence (PRODUCT #56) — reads the existing `DecaRouteIntel` rows (DATA #45, written on every DeCA creation), groups by `routeKey` over a capped most-recent-first window, returns top-N routes with count + most-recent DeCA id (powers "quick create from route") |
| listTemplates() / createTemplate() / setTemplateFavorite() / deleteTemplate() | function | lib/data/templates.ts | docs/reference/lib.md | Reusable DeCA templates — recurring non-date data per lane (UX #25); never carries a token or a transport date |
| templatePayloadSchema / TemplateInput / TemplateRow | schema/type | lib/data/template-schema.ts | docs/reference/lib.md | Split out of templates.ts (which is `server-only`) so the pure schema stays unit-testable, mirroring saved-schema.ts/saved.ts. `shipments?` (#113 §5) — envíos BEYOND the first, for a whole recurring multi-envío lane saved from an already-generated DeCA; reuses `lib/deca/schema.ts`'s `shipmentSchema` verbatim, dates always stripped |
| GET/POST /api/templates, DELETE /api/templates/[id] | route | app/api/templates/ | docs/reference/endpoints.md | Company-scoped template CRUD; any authed non-`read_only` member (PRODUCT #56; 401 unauthenticated, 403 `read_only`) |
| lib/team.ts (createInvite / acceptInvite / consumeInviteToken / listMembers / removeMember / changeRole / canWrite / getInvitePreview / TeamError) | module | lib/team.ts | docs/reference/lib.md | Company workspaces + invitations (TEAM #27/#37, PRODUCT #56); owner=Company Admin / member=Operator / read_only=Auditor (view-only, `canWrite()` gates every mutating route); invite tokens stored hashed, 14-day TTL, one-time; changeRole() promotes/demotes and never drops the last admin (any non-owner target role, not just member). `acceptInvite()` and `signup()`/`completeCompanyForUser()`'s invite branches (`lib/auth/index.ts`) all reject a redemption whose account email doesn't match the invite's (`invite_email_mismatch`, #124) |
| PATCH /api/team/members/[id] | route | app/api/team/members/[id]/route.ts | docs/reference/endpoints.md | Admin changes a member's workspace role (TEAM #37); 403 non-admin, 422 last-admin / self |
| POST /api/team/invites, DELETE /api/team/invites/[id], DELETE /api/team/members/[id] | route | app/api/team/ | docs/reference/endpoints.md | Admin-only workspace management; 403 for non-admins. `POST /api/team/invites` is rate-limited (`checkAbuse("share", ...)`, #129, D-227) — an authenticated member can't email-bomb arbitrary addresses |
| lib/growth.ts (createProspect / issueProspectInvite / resolveProspectInvite / attachCompanyToProspect / touchProspectActivity / acquisitionFunnel / importProspects) | module | lib/growth.ts | docs/reference/lib.md | Company acquisition funnel (GROWTH #28) — prospects, operator-attributed onboarding links, activation = first DeCA |
| POST /api/operadores/prospects (create/invite/import) | route | app/api/operadores/prospects/ | docs/reference/endpoints.md | Internal-only (404 for others); seed prospects, issue onboarding links, bulk import |
| POST /api/auth/password/request, POST /api/auth/password/reset, POST /api/auth/logout | route | app/api/auth/ | docs/reference/endpoints.md | Password recovery (no enumeration; test-seam FVD_EXPOSE_RESET_TOKEN) + logout (ACCOUNT #23) |
| rowMatches() | function | lib/data/history-filter.ts | docs/reference/lib.md | Pure history filter predicate: free text + date range + exact carrier + plate contains (WORKSPACE #24) |
| DatosHabitualesManager / AppNav | component | components/app/* | docs/reference/lib.md | Workspace nav + tabbed saved-data CRUD UI (#113 Phase 2, D-208 — renamed from `SavedDataManager`; Empresas/Vehículos/Lugares/Rutas tabs, global search, `Modal`-based creation, §13 duplicate warnings) |
| Modal | component | components/app/modal.tsx | docs/reference/lib.md | #113 §7, D-208 — generic focus-managed overlay (role="dialog", Escape/backdrop close), extracted from command-palette.tsx's existing pattern; used by every "+ Añadir" flow in Datos habituales |
| findDuplicateCompany() / findDuplicateVehicle() / findDuplicateLocation() / findDuplicateShipment() | function | lib/data/saved-dedup.ts | docs/reference/lib.md | #113 §13, D-208 — pure, client-side exact-match duplicate detection per kind, same normalization each schema applies on save; caller decides warn-vs-block (always warn, never block) |
| GET /api/operadores/stats | route | app/api/operadores/stats/route.ts | docs/reference/endpoints.md | Per-operator acquisition stats; internal role only (404 otherwise) |
| parseTouch() / touchIsQualifying() / UTM_KEYS | fn/const | lib/attribution/parse.ts | docs/reference/lib.md | Pure: extract an acquisition touch (ref + 5 UTMs) + classify channel |
| mergeTouch() / mergeFromUrl() / lock() / toAcquisitionRow() | function | lib/attribution/merge.ts | docs/reference/lib.md | Pure: first-touch-never-overwritten + last-touch merge rules (F12) |
| captureAttribution() / lockAttribution() | function | lib/attribution/client.ts | docs/reference/lib.md | Client cookie+localStorage capture; lock at signup |
| writeAcquisitionAtSignup() / markFirstDeca() / operatorStats() | function | lib/attribution/persist.ts | docs/reference/lib.md | Server: write the acquisition row at signup, first_deca_at, per-operator report |
| AttributionCapture | component | components/analytics/attribution-capture.tsx | docs/reference/lib.md | Root-layout invisible touch recorder |
| POST /api/deca/[id]/version | route | app/api/deca/[id]/version/route.ts | docs/reference/endpoints.md | Correct a DeCA → new version (R-13); any authed non-`read_only` company member (PRODUCT #56); 404/403/422. Same `decaPayloadSchema` as creation, so a correction can also add/remove/edit shipments (#112) — the wizard itself does not yet expose this for an already-multi-shipment DeCA (Sprint 2) |
| POST /api/share | route | app/api/share/route.ts | docs/reference/endpoints.md | Email the DeCA link to the driver (rate-limited, templated); mailto fallback when Resend unconfigured |
| correctDeca() / DecaCorrectionError | fn/class | lib/deca/persist.ts | docs/reference/lib.md | Append a new DeCA version (new token/URL/QR/PDF), keep priors, record reason + timestamp + author (userId) + pdfSha256. `serviceStart`/`serviceEnd` (R-9 availability window) cover the min/max resolved date across every shipment, not just the DeCA-level default (#112) |
| getDecaDetail() | function | lib/data/history.ts | docs/reference/lib.md | Company-scoped DeCA detail + version history (per-version reason, UTC timestamp, correcting-user email) |
| decide() / POLICIES / windowStart() | fn/const | lib/abuse/limiter.ts | docs/reference/lib.md | Pure sliding-window rate decision (allow/challenge/block) |
| checkAbuse() / abuseKey() | function | lib/abuse/index.ts | docs/reference/lib.md | Server rate check; records the attempt; never called by /d/ |
| challengePrefix() / verifyPow() / verifyHcaptcha() | function | lib/abuse/challenge.ts | docs/reference/lib.md | Signed PoW challenge (plain SHA-256, client needs no secret) + hCaptcha verify |
| abuseResponse() | function | lib/abuse/response.ts | docs/reference/lib.md | 429 challenge / block HTTP response builder |
| clientFingerprint() / solveChallenge() | function | lib/abuse/client.ts | docs/reference/lib.md | Client: cheap abuse signal + PoW solver (Web Crypto) |
| sendMail() | function | lib/mailer.ts | docs/reference/lib.md | Resend transactional email; `{sent:false, reason:"unconfigured"}` fallback. Provider error-body log redacts PII via `redactPii()` before it's written (#127) |
| redactPii() | function | lib/text/redact.ts | docs/reference/lib.md | Shared log-redaction helper — strips emails + long identifier-looking digit runs; extracted from `lib/deca/generation.ts`'s `safeErrorSummary()` so a second log call site doesn't duplicate the regex (#127) |
| /[slug] (SEO cluster) | route | app/(seo)/[slug]/page.tsx | docs/reference/endpoints.md | 10 static SEO pages (dynamicParams=false); FAQPage JSON-LD, canonical, CTA |
| /soy-obligado | route | app/(seo)/soy-obligado/page.tsx | docs/reference/endpoints.md | "¿Estoy obligado?" guided page — SSR query-param form, works without JS (F17) |
| SEO_PAGES / getSeoPage() | const/fn | content/seo/pages.ts | docs/reference/lib.md | The 10 SEO pages' content (intent, sections, faq, sources, related) |
| middleware() | function | middleware.ts | docs/reference/lib.md | CSP + HSTS + security headers on every HTML response (T-5/T-8) |
| register() | function | instrumentation.ts | docs/reference/lib.md | Next.js boot hook — calls `getEnv()` once per server instance (nodejs runtime only) so a missing/malformed required env var crashes every request instead of degrading silently (#123) |
| .githooks/pre-commit | hook | .githooks/pre-commit | docs/07-release.md | Confidential-data gate (`core.hooksPath=.githooks`) |
| CI workflow | workflow | .github/workflows/ci.yml | docs/07-release.md | typecheck/lint/format/unit/e2e/compliance/keel-verify + secret scan on main, tags, PRs to main |
| Dockerfile | build | Dockerfile | docs/07-release.md | Production image — Next standalone, non-root, healthcheck |
| hashIdentifier() / clientIp() | function | lib/hash.ts | docs/reference/lib.md | One-way IP hash + proxy IP extraction for the access log; throws via `requireHashSecret()` if `FVD_HASH_SECRET` is missing/short — no insecure fallback (#123) |
| validateDeca() / DecaValidationError | fn/class | lib/deca/validate.ts | docs/reference/lib.md | Full R-2 compliance validation; throws on missing mandatory field, warns (never blocks) on foreign NIF |
| decaPayloadSchema / step1..3Schema / formatPartyAddressLines() | const/fn | lib/deca/schema.ts | docs/reference/lib.md | zod schemas for the wizard steps + the full DeCA payload; `formatPartyAddressLines` composes a party's domicilio (street line, then "CP población" when informed) for the PDF/review, D-145. `decaPayloadSchema` accepts EITHER the flat legacy body or `{..., shipments: [...]}` and normalizes both into one canonical `DecaPayload` (#112, D-205) |
| shipmentSchema / resolveShipment() / resolveShipments() / legacyMirrorFields() / sumWeights() | schema/fn | lib/deca/schema.ts | docs/reference/lib.md | #112 — multiple shipments ("envíos") per DeCA. `shipmentSchema`: always-explicit `loadLocation`/`unloadLocation`/`goods`/`weight`/`recipient` + optional per-shipment overrides of the DeCA-level `loadDate`/`unloadDate`/`tractorPlate`/`trailerPlate`/`notes` defaults. `resolveShipment()`/`resolveShipments()` merge overrides onto defaults into the final effective value — the only thing the PDF/any consumer should read. `legacyMirrorFields()` returns shipment 1's resolved view in the pre-#112 flat shape (written into `dataJson` for backward-compat, D-205). `sumWeights()` sums numeric-parseable weights to kg (es-ES formatted); never fabricates a total when any shipment's weight is a non-numeric alternative measure. `canonicalSchema`'s `superRefine` rejects a per-shipment `tractorPlate`/`trailerPlate` that differs from the DeCA-level default once `shipments.length > 1` — vehicle is DeCA-level only, and the printed PDF never shows a per-shipment plate (#128, D-226) |
| normalizePlate() / looksLikeSpanishPlate() | function | lib/deca/plate.ts | docs/reference/lib.md | Plate normalisation + soft Spanish-format hint |
| checkNif() | function | lib/deca/nif.ts | docs/reference/lib.md | DNI/NIE/CIF control validation (advisory) |
| newPublicToken() / newClaimToken() | function | lib/deca/token.ts | docs/reference/lib.md | 256-bit base64url tokens for the public URL and the claim link |
| createDeca() | function | lib/deca/persist.ts | docs/reference/lib.md | Atomic persist of deca + version 1 (+ claim token for anonymous); returns pdfSha256; idempotency-key aware. `dataJson` stores the canonical `{shipper,carrier,...defaults,shipments}` shape PLUS `shipments[0]`'s resolved values mirrored at the top level in the pre-#112 flat shape, via `toDataJson()`/`legacyMirrorFields()` — every existing flat-shape reader (history/search/CSV/templates/route-intel/admin table) keeps working unchanged (#112, D-205) |
| CrearWizard / Field / ResultActions | component | components/deca/* | docs/reference/lib.md | 3-step creator UI + result-screen actions. `SavedData.shipments` (#113) — "usar ruta/envío habitual" picker on shipment 1 and every ENVÍO N block, fills origin/destination/goods/weight/recipient in one action |
| distinctPlaces() | function | components/deca/wizard.tsx | docs/reference/lib.md | #112 correction — exact-match dedup (name+city+address) of every load/unload place in use; gates the "Vincular carga y descarga" panel, shown only once both sides have 2+ distinct places (D-229) |
| SaveShipment | component | components/deca/save-shipment.tsx | docs/reference/lib.md | #113 §11 — "☆ Guardar como envío habitual", inline during DeCA creation; shown only once both legs are already `SavedLocation`-backed (mirrors SaveTemplate's open/confirm pattern) |
| GET /robots.txt | route | app/robots.ts | docs/reference/endpoints.md | Allow site; disallow /app /api /d/ /operadores /claim |
| GET /sitemap.xml | route | app/sitemap.ts | docs/reference/endpoints.md | Public indexable pages only |
| EVENT_NAMES / eventInputSchema / pickRefSnapshot() | const/fn | lib/analytics/events.ts | docs/reference/lib.md | Closed event set + ingest schema + ref/UTM extraction |
| track() / getSessionId() | function | lib/analytics/client.ts | docs/reference/lib.md | Client-side fire-and-forget analytics + opaque session id |
| landingJsonLd() + HERO/STEPS/BENEFITS/LEGAL_*/PLANS | fn/const | lib/content/landing.ts | docs/reference/lib.md | Landing copy + schema.org JSON-LD builder — emits `WebSite` + `SoftwareApplication`; deliberately never `FAQPage` (D-169, #98). `PLANS` = informational 2027 pricing figures for the `#planes` section (#109), no billing logic |
| CtaButton / SiteHeader / SiteFooter / MobileCta / DecaPreview / TrackView | component | components/site/*, components/analytics/* | docs/reference/lib.md | Landing UI building blocks |
| getEnv() | function | lib/env.ts | docs/reference/lib.md | Parse & cache the validated server environment (fail-closed); called at boot by `instrumentation.ts` (#123 — previously only invoked lazily, never enforced) |
| publicEnv | const | lib/env.ts | docs/reference/lib.md | Client-safe env values |
| requireHashSecret() | function | lib/env.ts | docs/reference/lib.md | The one shared getter for `FVD_HASH_SECRET`; throws if missing/<16 chars, no fallback (#123) — used by every session/CSRF-state/WebAuthn-challenge/PoW-challenge/backup-password signer instead of each duplicating the check |
| getSupabaseServerClient() | function | lib/supabase/server.ts | docs/reference/lib.md | Request-scoped Supabase client bound to auth cookies |
| getSupabaseServiceClient() | function | lib/supabase/server.ts | docs/reference/lib.md | Service-role Supabase client (server only, never user-authz) |
| getSupabaseBrowserClient() | function | lib/supabase/client.ts | docs/reference/lib.md | Browser Supabase client (anon key) |
| prisma | const | lib/prisma.ts | docs/reference/lib.md | Shared PrismaClient singleton |
| APP_VERSION | const | lib/version.ts | docs/reference/lib.md | Single source of the app version |
| LOCALES / LOCALE_NAMES / DEFAULT_LOCALE / LOCALE_COOKIE / isLocale() | const/fn | lib/i18n/locale.ts | docs/reference/lib.md | The 9-locale registry (es/ca/eu/gl/en/fr/de/it/pt, #116/D-211) — single source every dictionary import list, the switcher, and the `/api/i18n/locale` route derive from; `LOCALE_NAMES` gives each locale's native endonym |
| es / ca / eu / gl / en / fr / de / it / pt | const | lib/i18n/dictionaries/*.ts | docs/reference/lib.md | Per-locale string catalogs, each `satisfies Messages` against `es.ts`'s exact shape (a missing key fails the build); `es-ES` is the source of truth (D-002) |
| getLocale() / getDictionary() | function | lib/i18n/server.ts | docs/reference/lib.md | Server-side locale resolution (cookie → `DEFAULT_LOCALE` fallback, never `Accept-Language`) + the full per-locale dictionary lookup |
| LocaleProvider / useLocale() / useT() | component/hook | lib/i18n/client.tsx | docs/reference/lib.md | Client-side locale context, seeded from the server-resolved locale; `useT()` returns the current full dictionary |
| LanguageSwitcher | component | components/i18n/language-switcher.tsx | docs/reference/lib.md | The locale picker (dropdown, constant footprint regardless of locale count) — posts to `/api/i18n/locale`, patches `HEADER_STRINGS` client-side for static pages |
| GENERATION_STAGES / GenerationError / classifyError() / newCorrelationId() / safeErrorSummary() / stageMessage() | module | lib/deca/generation.ts | docs/reference/lib.md | Pure stage classification for DeCA generation failures (#29) + the short user-facing correlation code; `safeErrorSummary()` redacts PII from every logged message via the shared `redactPii()` (#127, extracted from here) |
| recordGenerationFailure() / markFailureRetried() / storageDriver() | function | lib/deca/failures.ts | docs/reference/lib.md | Log + persist a classified generation failure keyed by correlation code; never throws, never stores payload/PII |
| getDecaCockpit() / diffVersions() / CockpitData | module | lib/deca/detail.ts | docs/reference/lib.md | Shared data for the post-generation document cockpit (#36): sectioned summary, version history, field-level "what changed" diff; company-scoped when a companyId is passed, otherwise the anonymous result view. `diffVersions()` is shipment-aware (#112, D-206): shipment 1 stays covered by the existing flat-field diff (plus a new `recipient` row, the one field never mirrored to the top level); every shipment beyond the first is diffed as its own "Envío N" row — added/removed/goods/weight/recipient/route — never conflated with shipment 1's own fields |
| historyToCsv() / docWorkflowStatus() / WorkflowStatus | module | lib/deca/export.ts | docs/reference/lib.md | Company DeCA history CSV export (#34 §3, RFC 4180, BOM) + the product workflow status (Vigente / Corregida / No disponible — never a legal status). New `envios_totales` column (#112, D-206), added after `mercancia` — `lugar_carga`/`lugar_descarga`/`mercancia` keep describing shipment 1 only, so a script already parsing this CSV never breaks. `csvField()` also neutralizes a leading `=`/`+`/`-`/`@` with `'` — formula/CSV-injection protection for the free-text columns (#126) |
| GET /api/export/history | route | app/api/export/history/route.ts | docs/reference/endpoints.md | Streams the signed-in company's DeCA history as CSV (#34); company-scoped (T-1), honours the /panel/historico filter params; 401 for anonymous |
| runDiagnostics() / DiagnosticsReport | fn/type | lib/diagnostics.ts | docs/reference/lib.md | Production readiness probes: env, DB, schema, PDF render smoke, storage round-trip, base URL, providers, 24h generation health. Returns no secret |
| getInternalUser() / hasAdminToken() / isInternalRequest() / requireInternal() / requireStepUp() / isAdmin2faFresh() / isAdminStepUpFresh() / hasEnrolledStrongAuth() | function | lib/admin/guard.ts | docs/reference/lib.md | Internal-area authorization + mandatory strong 2FA (SECURITY #53 passkey follow-up, D-105): internal-role session with a fresh TOTP-or-passkey check (or FVD_ADMIN_TOKEN header, or a trusted-device cookie for routine access only — never for `requireStepUp()`), else 404 (never 403). `hasEnrolledStrongAuth()` = TOTP enabled OR ≥1 passkey. `isAdmin2faFresh()` = non-throwing 12h-window test; `isAdminStepUpFresh()` = non-throwing 10-min step-up-window test, used by `/admin/2fa/verify?stepup=1` so a stale step-up actually re-challenges instead of looping (D-194) |
| overviewMetrics() / operationalAlerts() / windowStart() | function | lib/admin/metrics.ts | docs/reference/lib.md | Admin overview KPIs (today/7d/30d) + operational alert rules, all from real rows (#33 §1) |
| listFailures() / getFailure() / failureStageCounts() / triageFailure() / failureTriageSchema | module | lib/admin/failures.ts | docs/reference/lib.md | Read + triage the #29 generation failures by correlation code; never returns payload/PII (#33 §7) |
| listDecaAdmin() / getDecaAdmin() / listCompaniesAdmin() / getCompanyAdmin() / listUsersAdmin() | function | lib/admin/records.ts | docs/reference/lib.md | Cross-tenant admin read models for DeCA / companies / users; only reached through requireInternal(); summarises document content, never returns it whole (#33 §2–§4). `DecaAdminRow.shipmentCount` (#112, D-206) drives the admin table's "+N envío(s)" indicator next to `route` |
| adminSearch() / SearchHit | fn/type | lib/admin/search.ts | docs/reference/lib.md | Admin global search across company / user / DeCA reference / correlation code / prospect (#33 §9) |
| rangeFromParam() / RANGE_KEYS | fn/const | lib/admin/range.ts | docs/reference/lib.md | Pure windowed date ranges (24h/7d/30d/90d) for the admin filters |
| recordAudit() | function | lib/admin/audit.ts | docs/reference/lib.md | Append-only SecurityAuditLog writer (SECURITY #53) — never throws, never edited/deleted elsewhere; called from auth, 2FA, content-edit and team-mutation code paths |
| listAuditLog() / distinctAuditActions() | function | lib/admin/audit-log.ts | docs/reference/lib.md | Read side of the SecurityAuditLog trail (SECURITY #53, PRODUCT #56) — filters by action/result/range, resolves actor emails; the sole writer stays lib/admin/audit.ts's recordAudit() |
| GET /api/admin/diagnostics | route | app/api/admin/diagnostics/route.ts | docs/reference/endpoints.md | Readiness report for deploy verification (`npm run diagnose -- <url>`); internal session or x-fvd-admin-token, 404 otherwise; 503 when a critical check fails |
| relyingParty() / buildRegistrationOptions() / verifyRegistration() / buildAuthenticationOptions() / verifyAuthentication() | function | lib/auth/webauthn.ts | docs/reference/lib.md | SECURITY #53 passkey follow-up (D-105): thin `@simplewebauthn/server` wrappers, RP ID/origin derived from `NEXT_PUBLIC_FVD_BASE_URL` (no new env var); `attestationType: "none"`, `preferredAuthenticatorType: "localDevice"` biases the browser toward Face ID/Touch ID/Windows Hello |
| createWebAuthnChallenge() / verifyWebAuthnChallenge() / WEBAUTHN_CHALLENGE_COOKIE | function | lib/auth/webauthn-challenge.ts | docs/reference/lib.md | Signed, stateless, 5-min-TTL cookie carrying a WebAuthn ceremony challenge (mirrors lib/auth/oauth-state.ts) — no throwaway DB table |
| registerPasskey() / authenticateWithPasskey() / browserSupportsWebAuthn() | function | lib/auth/webauthn-client.ts | docs/reference/lib.md | "use client" browser helpers wrapping `@simplewebauthn/browser`'s ceremony calls, with friendly Spanish error mapping (D-105) |
| createTrustedDevice() / isTrustedDevice() / listTrustedDevices() / revokeTrustedDevice() / revokeAllTrustedDevices() | function | lib/auth/trusted-device.ts | docs/reference/lib.md | "Trust this device" for 30 days after a fresh passkey/TOTP check (D-105); hashed-token-in-DB, reusable until expiry/revocation; `revokeAllTrustedDevices()` is called from `bumpSessionVersion()` so a password change/reset never leaves a standing bypass |
| POST /api/admin/2fa/enroll, POST /api/admin/2fa/enable, POST /api/admin/2fa/verify, POST /api/admin/2fa/regenerate-codes, DELETE /api/admin/2fa/totp | route | app/api/admin/2fa/ | docs/reference/endpoints.md | Admin TOTP fallback (SECURITY #53): start enrollment / confirm+enable (issues recovery codes) / challenge a login or step-up / regenerate recovery codes (step-up gated) / reset (disable, step-up gated, refuses if it's the last strong-auth method, D-105) |
| POST /api/admin/2fa/webauthn/register-options, POST /api/admin/2fa/webauthn/register-verify, POST /api/admin/2fa/webauthn/auth-options, POST /api/admin/2fa/webauthn/auth-verify | route | app/api/admin/2fa/webauthn/ | docs/reference/endpoints.md | Passkey registration + authentication ceremonies (D-105); register-verify issues recovery codes only on the account's first-ever strong-auth method |
| GET /api/admin/2fa/webauthn/credentials, DELETE /api/admin/2fa/webauthn/credentials/[id] | route | app/api/admin/2fa/webauthn/credentials/ | docs/reference/endpoints.md | List / remove the caller's own passkeys (D-105); DELETE is step-up gated and refuses to remove the last strong-auth method |
| POST /api/admin/2fa/trust-device, GET /api/admin/2fa/trusted-devices, DELETE /api/admin/2fa/trusted-devices/[id] | route | app/api/admin/2fa/trust*/ | docs/reference/endpoints.md | Issue / list / revoke a 30-day trusted-device grant (D-105); revoke is step-up gated |
| PATCH /api/admin/failures/[correlationId] | route | app/api/admin/failures/[correlationId]/route.ts | docs/reference/endpoints.md | Triage a #29 generation failure (toggle resolved, add internal note); internal session or x-fvd-admin-token, 404 otherwise; never edits the failure record itself |
| GET /api/admin/search | route | app/api/admin/search/route.ts | docs/reference/endpoints.md | Admin global search (`?q=`); internal session or x-fvd-admin-token, 404 otherwise; read-only |
| POST /api/admin/operadores · PATCH /api/admin/operadores/[id] | route | app/api/admin/operadores/ | docs/reference/endpoints.md | Create an operator (auto ref code) / edit + activate·deactivate (#86 p8); `isInternalRequest` → 404 otherwise |
| lib/admin/operators.ts (createOperator / updateOperator / listOperators / getOperator / operatorLink / operatorSchema) | module | lib/admin/operators.ts | docs/reference/lib.md | Operator/commercial/referral management (#86 p8) — individual `/registro?ref=<code>` link, first-touch attribution read from `Acquisition.firstRefCode`, deactivate never removes history |
| CORRIDORS / matchCorridors() / zoneOf() / endZone() / countryCode() / ZONE_LABEL | module | lib/commercial/corridors.ts | docs/reference/lib.md | In-code table of corridors of commercial interest for Farvertrans (#87/#88) + pure zone/corridor matching for a route (Spain regional zones, FR/Benelux/IT/DE/PT, well-known-city hints); no I/O |
| summariseActivity() / ActivitySummary | fn/type | lib/commercial/activity.ts | docs/reference/lib.md | Pure rolling-window activity summary over event dates (#87/#88): 7/30/60/90d counts, first/last seen, trend vs the prior 30 days, weekday histogram |
| lib/commercial/opportunity-model.ts (Opportunity / OpportunityFilter / applyOpportunityFilter / sortOpportunities / routeMatchesGeoDate / OPPORTUNITY_STATES / opportunityUpdateSchema) | module | lib/commercial/opportunity-model.ts | docs/reference/lib.md | Types + pure filter/sort/validation for the `Oportunidades` radar (#87); no I/O |
| listOpportunities() / deriveOpportunity() / setOpportunityState() | function | lib/commercial/opportunities.ts | docs/reference/lib.md | `Super Admin > Oportunidades` radar (#87): eligible carriers (`CommercialConsent.mode != none`) joined with `DecaRouteIntel` + `Acquisition` + `CommercialOpportunity`; `setOpportunityState()` writes the manual follow-up state (audited); refuses a non-eligible company |
| buildCarrierProfile() / affinityScore() / autoTags() / CarrierCommercialProfile | module | lib/commercial/carrier-profile.ts + lib/commercial/affinity.ts | docs/reference/lib.md | Carrier commercial profile for `/admin/empresas/[id]` (#88): activity, top routes, frequent zones, recurring plates + a transparent rule-based "Afinidad Farvertrans" score with a per-rule breakdown and objective auto-tags |
| commercialKpis() / rollupFunnel() / maxProgress() / recentCommercialActivity() / carrierCommercialActivity() | module | lib/commercial/kpis.ts | docs/reference/lib.md | `Super Admin > Panel comercial` KPIs (#89): the DeCA→oportunidad→contacto→conversión→facturación funnel from own rows; attribution split (acquisition ref code vs internal commercial user); filters by period/operator/country/corridor; plus the global + per-carrier commercial activity trail |
| evaluateAlerts() / ALERT_RULES / refreshAlerts() / listAlerts() / setAlertStatus() / getAlertConfig() / saveAlertConfig() | module | lib/commercial/alert-rules.ts + lib/commercial/alerts.ts | docs/reference/lib.md | Internal rule-based commercial alerts (#90): 10 pure rules over own data, de-duplicated by company+kind+week; `refreshAlerts()` reconciles and never resurrects a reviewed/dismissed alert; only for consented companies |
| PATCH /api/admin/oportunidades/[companyId] | route | app/api/admin/oportunidades/[companyId]/route.ts | docs/reference/endpoints.md | Set the commercial follow-up state / note / channel / manual outcome for a carrier (#87/#89); `isInternalRequest` → 404; 409 `not_eligible`; writes the activity trail + `convertedBy*` on conversion |
| PATCH /api/admin/alertas-comerciales/[id] · POST /api/admin/alertas-comerciales/config | route | app/api/admin/alertas-comerciales/ | docs/reference/endpoints.md | Mark a commercial alert reviewed/dismissed / save the alert rule config (#90); `isInternalRequest` → 404 otherwise |
| GET·POST /api/panel/vistas | route | app/api/panel/vistas/route.ts | docs/reference/endpoints.md | The current USER's saved Histórico views (#92): list, and create one from the filters currently applied; 401 unauthenticated, 422 `invalid_name`, 409 `duplicate_name`/`limit_reached` |
| PATCH·DELETE /api/panel/vistas/[id] | route | app/api/panel/vistas/[id]/route.ts | docs/reference/endpoints.md | Rename or delete one saved view (#92); scoped by `(id, userId)`, so another user's id is 404, never 403 |
| PUT /api/panel/accesos | route | app/api/panel/accesos/route.ts | docs/reference/endpoints.md | Set this USER's up-to-3 quick accesses on Inicio (#93); the body is normalised through `parseQuickActions` (unknown keys, repeats and a 4th choice are dropped, never stored); 401 unauthenticated |
| HISTORY_FILTER_KEYS / filtersFromParams() / hasActiveFilters() / viewQuery() / viewMatches() / historyViewName() / describeFilters() | const/fn | lib/data/history-views.ts | docs/reference/lib.md | Pure logic for #92 saved views — the filter set is exactly the Histórico's own (a unit test pins it); builds the canonical query string, decides the active chip, validates a name, summarises a view with caller-supplied labels |
| listViews() / createView() / renameView() / deleteView() | function | lib/data/saved-views.ts | docs/reference/lib.md | Saved-view persistence (#92), PER USER — every query is scoped by `userId`, so an id alone is never enough to reach a row |
| QUICK_ACTIONS / DEFAULT_QUICK_ACTIONS / MAX_QUICK_ACTIONS / parseQuickActions() / resolveQuickActions() | const/fn | lib/panel/quick-actions.ts | docs/reference/lib.md | The #93 catalogue (10 existing destinations, no duplicates — `rutas`/`/panel/datos#rutas` added #113 Phase 2, D-208, superseding D-156's now-false "no such destination" premise) + normalisation of an untrusted selection + what Inicio renders for an unconfigured user |
| SavedViews / QuickActions | component | components/panel/{saved-views,quick-actions}.tsx | docs/reference/lib.md | #92 chip row above the Histórico filters (save/apply/rename/delete) and the #93 "Accesos rápidos" block with its inline Personalizar picker |
| GET·PUT /api/team/companies | route | app/api/team/companies/route.ts | docs/reference/endpoints.md | #102 — list this user's memberships (workspace switcher) / switch the active company; PUT 404 if the user has no membership in the target company |
| joinCompany() / leaveCompany() (internal) / listMembers() / listUserMemberships() / switchActiveCompany() / createInvite() / acceptInvite() / removeMember() / changeRole() | function | lib/team.ts | docs/reference/lib.md | #102 — Membership is the source of truth for company membership; `joinCompany`/`leaveCompany` are the only two places that touch both `Membership` and the active-company denormalization together |
| pickFallbackMembership() / canWrite() / CompanyRoleValue | fn/type | lib/team-membership.ts | docs/reference/lib.md | #102 — pure logic extracted out of `lib/team.ts` (server-only) so it is unit-testable: which remaining membership becomes active when the current one is removed |
| reassignUserToCompany() | function | lib/admin/lifecycle.ts | docs/reference/lib.md | #102 Superadmin recovery tool — reassociates an existing user to an existing company via `joinCompany`, audited with a mandatory reason; never creates a company, never touches another membership |
| MembershipReassign | component | components/admin/membership-reassign.tsx | docs/reference/lib.md | #102 — the Superadmin user-ficha form for `reassignUserToCompany` |
| IncidentPage | component | components/errors/incident-page.tsx | docs/reference/lib.md | #118 — shared branded "Lo sentimos" incident screen for `app/error.tsx`/`app/global-error.tsx`; zero DB/network dependency (static `es` copy only), reuses `Wordmark`/`.auth-ground` |
| app/error.tsx / app/global-error.tsx | error boundary | app/error.tsx, app/global-error.tsx | docs/reference/lib.md | #118 — route-segment and root-layout error boundaries; render `IncidentPage`, log `error.digest`/stack server-side only, never expose technical detail to the user |
| app/test-only/error-boundary | route | app/test-only/error-boundary/page.tsx | docs/reference/lib.md | #118 — E2E-only real render-time throw, gated by `FVD_ENABLE_TEST_ROUTES` (test seam, never set in production); exercises the real `app/error.tsx` boundary in Playwright |
